# Daily — 2026-05-03

## Session 013 — BUILD-11 (+ 11.5 / 11.6 / 12) smoke verification

Goal: empirically verify the payslip-extraction layer in production after
the four-commit chain (`6dfd881` → `f8cc0d4` → `ca8ad26` → `77ad656`) shipped
without a closed smoke pass. Discipline: CAL-004 — verify deployed behaviour
before stacking further work.

Driven via Playwright MCP + Supabase MCP + Vercel MCP. Account
`jo…@protonmail.com`, worker `85e2e02f-ab0a-47fe-aac8-bf5009c4b626`.

## Final result: **PASS**

Priority order executed E → C' → A → B → D. Stop on first failure rule held
throughout. No path failed.

| # | Path | Verdict | Key evidence |
|---|---|---|---|
| E | Extraction failure path | **PASS** (direct-API) | Synthetic doc `cbb855c8-…` with non-existent storage_path; `failExtraction` inserted `payslip_facts` row with `status='failed'`, `error='Storage download failed: Object not found'`, `model=claude-sonnet-4-6`, `prompt_version=extract-payslip-v02`. Vercel `[extract] payslip_failed` log fired. Cleanup restored baseline. |
| C' | Inline edit Hours (BUILD-12) | **PASS** | `pf 06de0a40-…` `ordinary_hours` 42 → 38 via UI Edit/Save. `payslip_facts_history` row `c9dbcbe7-…` captured pre-change snapshot. `updated_at` matched history `changed_at` to the microsecond. UI re-rendered Hours=38 + Edit affordance returned. |
| A | Extraction auto-trigger | **PASS** | `A_fresh_payslip.pdf` (3287 B, hash `2c3f869a…`) uploaded → POST `/api/classify` 200 + POST `/api/extract` 200; new doc `5173099e-…`, new case `8057aae3-…`, new `pf c32e029c-…` with `status='extracted'`. Upload→extracted ≈ 20s. v02 fields populated (employer "Sunset Coast Hospitality Pty Ltd", 3 earnings rows). |
| B | Confirm flow (v02 prompt) | **PASS** | Tapped [Looks right] on `pf c32e029c-…` → `status='extracted'` → `confirmed`, `confirmed_at=2026-05-03 00:40:21.645+00`. Integrity CHECK passed. Employer + 3 earnings persisted intact. UI flipped to ConfirmedCard ("✓ We found these values (confirmed)" + "Confirmed by you"). Audit trigger fired (history row `da6f7438-…`). |
| D | Non-payslip unaffected | **PASS** | `D_nonpayslip_artefact.png` (49725 B, screenshot of /cases) uploaded → classify 200 with `detected_type='other'`, `confidence=0.05`, `routing_status='failed'`. **No /api/extract call** in browser network or Vercel logs. **0 payslip_facts rows** for this `source_doc_id`. UI rendered BUILD-09 failure-guidance card. |

## Carry-forward findings (filed, not actioned this session)

- **CAL-005 verification gap confirmed.** Path E was not reachable from UI smoke (classify pre-filtering catches everything Anthropic itself rejects, before extract is invoked). Required synthetic-doc + direct API to exercise `failExtraction`. Same class as the BUILD-11 0017 silent-failure incident. Future BUILD-N work introducing failure modes should ship with a documented direct-API smoke recipe.
- **`payslip_facts_history` schema drift.** `log_psf_history` audit trigger snapshots only the pre-Migration-0016 columns; rows added today have `extraction_status` / `case_id` / `pay_date` / `extracted_at` / `extraction_jsonb` as NULL in history. Audit completeness is partial.
- **Extract is not idempotent on failures.** Path E retry produced two identical failure rows. Idempotency window is `(source_doc_id, status IN ['extracted','confirmed'])` — failures aren't included.
- **POL-014 candidate evidence reproduced.** First path E API call from browser hit `ERR_SSL_PROTOCOL_ERROR` while server-side request still executed correctly. Same shape as prior Norton/schannel observations.
- **Edit-after-confirm reset deferred.** `pf c32e029c-…` is now a confirmed candidate to test the ADR-012 edit-after-confirm reset flow if/when wanted.

## State left in production

- Worker docs: 10 → 12 (+1 path A payslip, +1 path D screenshot)
- Worker payslip_facts: 3 → 4 (+1 path A→B confirmed row)
- Worker active cases: 2 → 4 (+1 "Payslip", +1 "Other")
- `pf 06de0a40-…` (path C' target): `ordinary_hours` 42 → 38 (worker correction left in place — matches breakdown's ordinary line)
- `pf c32e029c-…` (path A→B target): confirmed
- Path E synthetic doc: cleaned up, baseline restored
- `payslip_facts_history`: +2 rows (path C' UPDATE, path B confirm UPDATE)

## Test artefacts (kept for re-run)

`.playwright-mcp/smoke-artefacts/`
- `E_corrupt_truncated.pdf` (600 B; truncated PDF — classify-rejected; not load-bearing for path E)
- `A_fresh_payslip.pdf` (3287 B; mock_payslipV2 + appended unique bytes)
- `D_nonpayslip_artefact.png` (49725 B; Playwright screenshot of /cases)
- `Cprime_01_edit_form.png` (UI evidence for path C')

## Next

Move to ISS-023 (overbroad extend-failure fallback) + ISS-022 (compound-failure logging + UX copy).
