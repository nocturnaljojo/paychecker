# 2026-04-30 — Build day (in progress)

## [Sprint B1.5 — Fix ensureWorker silent hang]

- **Time:** ~50 min / 60 min
- **Output:**
  - `.claude/STATE-PRJ-issues.md` — added ISS-005 entry (FIXED)
  - `src/lib/upload.ts` — `ensureWorker()` JSDoc + worker-friendly error wrapping
  - `src/features/upload/useUploadBatch.ts` — `startUpload()` now fails queued files loudly when worker resolution is missing
  - `src/features/upload/UploadZone.tsx` — banner copy + `/onboarding` recovery link + buttons gated on `workerError`
- **ISS-005 → FIXED** (option (a) AUTO-CREATE + caller / UI hardening)
- **ensureWorker behavior:** unchanged in success path — still SELECT-then-INSERT-on-miss. Schema-safe (only `clerk_user_id` is NOT NULL without a default; `tier` defaults to `'palm_free'`, `preferred_language` to `'en'`, timestamps default to `now()`). Errors now wrap with `Couldn't load your account: …` / `Couldn't set up your account: …` so the UI surface doesn't leak raw Postgres codes.
- **UI error surface:** when worker resolution fails, `Choose files` and `Take a photo` are disabled (previously only gated on `isResolvingWorker`, which cleared on error). The alert banner above the drop zone now uses worker-friendly copy ("We couldn't set up your upload area — try refreshing"), explains the next step, and links to `/onboarding` for users who haven't finished account setup. Belt-and-braces: if a queued file ever lands in `startUpload` without a `workerId` (race / programmatic call), the row pill now flips to "Couldn't read" with `Account not ready — try refreshing.` instead of hanging at "Waiting".
- **Sprint 7 onboarding flow:** UNCHANGED. `completeOnboarding` still calls the same `ensureWorker`, still UPDATEs `display_name` / `country` / `preferred_language`, still INSERTs `consent_records`. `useEmploymentFact` (Sprint 7's manual-fallback bucket) also still calls `ensureWorker` with the same signature.
- **Sprint SMOKE-001 unblocked:** ready to resume the 10-test smoke walk over the full B1+B2 pipeline.
- **Verification:** `npm run lint` clean. `npm run build` clean (`tsc -b` + `vite build` both pass; bundle size unchanged within rounding).
- **Commit:** `2d02edd`.

## [Sprint B1.6 — Upload-time consent gate]

- **Time:** ~50 min / 75 min
- **Output:**
  - NEW: `src/components/layout/ConsentRequired.tsx` — route-guard wrapper that redirects to `/onboarding` when `consent_records` is missing; soft-fails OPEN to `/onboarding` on transient query errors (compliance-bias).
  - MODIFIED: `src/pages/Upload.tsx` — wrapped in `<ConsentRequired>`.
  - MODIFIED: `src/pages/EmploymentContract.tsx` — wrapped in `<ConsentRequired>` (Option 1 scope extension; APP 1 applies to manual-fallback fact-entry route too).
  - MODIFIED: `api/classify.ts` — Layer (c) server-side check between worker resolution and Anthropic call; 403 with `error: 'consent_required'` if `consent_records` row count is 0.
  - MODIFIED: `src/features/upload/useClassifyBatch.ts` — `ClassifyStatus` union extended with `'consent_required'`; 403 decoder branches to that status with message passthrough.
  - MODIFIED: `src/features/upload/UploadZone.tsx` — `CombinedStatus` extended; new amber pill `"Privacy setup first"`; new top-level banner `"Some files couldn't be read until your privacy setup is done."` with `[Complete setup]` button to `/onboarding`; `summarise()` includes consent_required count.
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-006 entry (FIXED).
  - LOGGED: `.claude/STATE-PRJ-improvements.md` — POL-008 entry (PLANNED for Phase 1).
- **ISS-006 → FIXED** (Layer a route guard + Layer c server-side check shipped together; covers BOTH `/upload` AND `/buckets/employment-contract`)
- **POL-008 → PLANNED** (Phase 1 RLS predicate on `documents_self_insert` for defense-in-depth)
- **R-010 mitigation:** `consent_records` existence verified client-side via route guard AND server-side via `/api/classify` 403 before any document content reaches Anthropic API. `worker_id` filter on the consent SELECT uses the JWT-verified id from the server, never a client-supplied value.
- **APP 1 + APP 6 alignment:** privacy policy v1 commits to consent before processing (`docs/legal/privacy-policy-v1-draft.md` §3, §6); B1.6 enforces that commitment in code. Manual-fallback path (`/buckets/employment-contract`) covered for APP 1 even though no cross-border transfer.
- **Worker copy:** calculator-language only — `"Privacy setup first"`, `"Some files couldn't be read until your privacy setup is done."`, `"Complete setup"`. Never `"consent missing"`, `"Privacy Act"`, `"GDPR"`.
- **Sprint 7 onboarding flow:** UNCHANGED. `OnboardingFlow.tsx` + `complete.ts` + `Step6Consent.tsx` all untouched. Only the route pages got wrapped.
- **Schema preserved:** No migration files modified. `consent_records` immutability triggers (`reject_consent_mutation`) still in place. POL-008 captured for the future migration that adds the RLS predicate.
- **Stack note:** ConsentRequired sits inside ProtectedRoute via existing route definitions (`App.tsx:47-53`, `App.tsx:39-46`). Only an `isLoaded` early-return guard inside ConsentRequired — no redundant Clerk re-check, keeps the gate composable.
- **Verification:**
  - `npm run lint` clean
  - `npm run build` clean (`tsc -b` + `vite build`; 1718 modules transformed (1 added for ConsentRequired); bundle 525.80 kB / gzip 149.50 kB, +1.4 kB raw / +0.18 kB gzipped — entirely the new component + small union extensions)
  - `grep -r "@anthropic-ai/sdk" src/` → 0 matches (server/client security boundary holds)
  - `grep -r "@clerk/backend" src/` → 0 matches (server/client security boundary holds)
- **Sprint SMOKE-001 unblocked:** pipeline now compliant; end-to-end smoke walk can run with the consent gate enforcing R-010.
- **Commit:** `fefb42f`.

## [Sprint B1.9 — Migration 0013: storage RLS PUBLIC pattern (ISS-009 close)]

- **Time:** ~40 min / 60 min (audit ~10 min reused mostly from earlier ISS-009 investigation context; build ~25 min including MCP-retry attempts; verify deferred to post-apply)
- **Output:**
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-009 entry (status WRITTEN, apply pending)
  - LOGGED: `.claude/STATE-PRJ-improvements.md` — POL-012 entry (Phase 1+ — Clerk JWT template config as defense-in-depth alternative)
  - NEW: `supabase/migrations/0013_storage_rls_public_pattern.sql` — drops 6 storage policies (3 documents + 3 payslips), recreates with PUBLIC scope + JWT-presence guard, mirroring `public.documents` pattern. ROLLBACK section captures verbatim TO-authenticated originals from Migrations 0011 + 0003.
  - NOT YET APPLIED — Supabase MCP returned `"Authentication service is temporarily unavailable"` on every attempt during this sprint (apply_migration + execute_sql + connectivity check all hit the same overload error). Migration file is committed; apply is a follow-up operational step.
- **ISS-009 → WRITTEN.** Migration 0013 ready for application. Three apply paths: (a) Supabase Studio → SQL Editor → paste 0013 contents → Run (~30 seconds); (b) retry `apply_migration` via MCP when the service recovers; (c) Supabase CLI `supabase db push` if available locally.
- **POL-012 captured.** Phase 1+ defense-in-depth alternative — configure Clerk JWT template to include `role: 'authenticated'` claim, then storage policies could revert to Supabase-default `TO authenticated` scope. Either pattern works for security; POL-012 is the Supabase-idiomatic of the two. Not urgent; Migration 0013 closes ISS-009 fully.
- **Pattern signal:** Five sprints today (B1.5 → B1.6 → B1.7 → B1.8 → B1.9) all on the upload pipeline; each one unmasked the next bug. ISS-009 was hidden by ISS-008 was hidden by ISS-007 was hidden by ISS-005. SMOKE-001 was the right next sprint to schedule but couldn't run because each unmasking surfaced fresh blockers. Lesson: **browser-smoke each sprint that touches a new pipeline stage**, even when lint+build pass.
- **Audit findings:**
  - Migration 0011's `documents_storage_self_*` policies and Migration 0003's `payslips_self_*` policies are all scoped `TO authenticated` (6 policies total).
  - `public.workers` / `public.documents` / `public.consent_records` policies all use no-`TO`-clause (PUBLIC), which is why `auth.jwt() ->> 'sub'`-keyed SELECTs work for the same Clerk JWT that storage rejects.
  - Migration 0012 precedent: DROP + CREATE pattern, `(SELECT auth.jwt() ->> 'sub')` wrap for planner-init-plan caching, verbatim ROLLBACK as commented SQL. Mirrored exactly in 0013.
- **Pressure-tests verified in policy logic** (apply pending):
  1. Anonymous request → JWT guard `IS NOT NULL` fails → INSERT denied. ✓
  2. Cross-tenant attempt → `current_worker_id()` returns caller's UUID, not the path's → mismatch → INSERT denied. ✓
  3. Expired JWT → rejected at JWT validation layer (401) before RLS evaluates. ✓
- **Verification queries to run post-apply:** confirm `polroles: {0}` (PUBLIC) for the 6 storage policies, confirm `pg_get_expr(polqual/polwithcheck)` includes both JWT guard and `current_worker_id()` foldername check, confirm `public.*` policies are unchanged (still PUBLIC, still bare `current_worker_id()`).
- **Sprint SMOKE-001 unblocking:** pending apply. Once 0013 lands, the storage upload should succeed for any signed-in worker whose JWT carries a `sub` claim and whose `workers.clerk_user_id` matches; the rest of the pipeline (`documents` INSERT, `/api/classify`, routing) was confirmed working in the diagnosis.
- **Verification:** lint + build n/a (no app code changed). Migration file has no syntax issues by inspection (mirrors Migration 0012 exactly in shape). Apply-time verification deferred to post-apply.
- **Commit:** `0a65d5b`.

### Post-apply addendum (same sprint window)

- **Apply:** Supabase MCP recovered after the initial outage. Migration applied via `apply_migration` (`0013_storage_rls_public_pattern`) — `{success: true}`.
- **Verification — all four queries green:**
  1. **Role scope flipped to PUBLIC.** All 6 storage policies (3 `documents_storage_*` + 3 `payslips_self_*`) now show `polroles: {PUBLIC}` (was `{authenticated}` pre-apply).
  2. **Policy expressions intact.** Every policy's `USING`/`WITH CHECK` includes all three clauses: `bucket_id` match + `(SELECT auth.jwt() ->> 'sub') IS NOT NULL` + `(storage.foldername(name))[1] = (SELECT current_worker_id())::text`.
  3. **`public.*` policies UNCHANGED.** `documents_self_*`, `workers_self_*`, `consent_records_self_*` all still `{PUBLIC}`. Migration 0013 did not touch them.
  4. **Performance advisor — no new lints.** The 8 `auth_rls_initplan` warnings are the pre-existing stale-cache from POL-002 (Migration 0012 rewrote those policies correctly; advisor cache hasn't refreshed). The `unused_index` warnings are pre-existing per ISS-004. None caused by 0013.
- **ISS-009 → FIXED.** Status flipped from `WRITTEN — apply pending` to `FIXED` in `.claude/STATE-PRJ-issues.md`.
- **SMOKE-001 unblocked.** First-click upload should now flow `pending → uploading → uploaded → reading → saved` (or `→ needs your check`) end-to-end. Storage RLS no longer denies; documents INSERT runs (PUBLIC policy was already correct); `/api/classify` runs (service-role bypasses RLS). If the pill flips to anything other than `saved` / `needs your check` / `couldn't read` (with a non-RLS reason), it's a different bug we triage fresh.
- **Apply-addendum commit:** `d51e630`.

## [Sprint B1.10 — Anthropic timeout + tactical diagnostics (ISS-010 close)]

- **Time:** ~30 min / 45 min
- **Output:**
  - MODIFIED: `api/classify.ts` — Anthropic constructor now passes `timeout: 30_000` + `maxRetries: 1` (worst-case 60s wall vs prior 30min default); 27 `[classify]` log lines distributed across every checkpoint + error path (entry, post-auth, post-resolve, post-consent, post-doc-load, post-state-update, post-storage-download, anthropic-start with `t0=Date.now()`, anthropic-end with elapsed-ms, parsed result, classification INSERT, each terminal return, plus error logs in `callClassifier` catch and `failClassification`).
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-010 entry (FIXED).
  - LOGGED: `.claude/STATE-PRJ-improvements.md` — POL-013 entry (project convention: every external API boundary needs explicit timeout + diagnostic logs going forward).
- **ISS-010 → FIXED.** Two-part fix shipped.
  - **Part 1 (resilience):** Anthropic ctor now bounded. SDK default of 600,000ms × 2 retries = 30min worst case → bounded to 30s × 1 retry = 60s worst case. On timeout, existing `callClassifier` try/catch returns `{ error }` → `failClassification` → 200 with worker-readable reason. Clean degradation.
  - **Part 2 (observability):** 27 log lines distributed through the handler. Privacy-safe per `REF-PRIVACY-baseline.md` rule 1: UUIDs + metadata only. Clerk user_id truncated to first 8 chars. No document content. No PII beyond UUIDs. Includes `console.error` in every failure branch + the `callClassifier` catch + `failClassification` so the operator's terminal surfaces the actual SDK/Postgres error message before any 500/200-failed response.
- **POL-013 captured.** Project-wide convention going forward: every external-API-calling function must have (1) explicit timeout, (2) tactical `console.log` at major checkpoints, (3) `console.error` before failure responses, (4) bounded `maxRetries`. Apply forward to `api/extract.ts` (Sprint B3), any new `api/*` function. Open question whether to wrap into a helper (decide when second `api/*` function lands).
- **Pattern signal:** This is the second time today (after ISS-008 silent stall) that a pipeline component had hung silently with no diagnostic surface. Both were "the function awaits an external boundary that never resolves; no logs to tell us." Diagnostic gaps are their own defects. POL-013 captures the lesson as a convention. Six sprints today (B1.5 → B1.10), six closed P1 issues, one ratified architectural convention.
- **What this fix does NOT determine:** WHY today's specific 500 occurred. The next SMOKE-001 attempt will tell us in <5 seconds via terminal output. Most likely candidates: (a) Anthropic API key issue (recently rotated per SEC-001 era; the user has `.env.local` open in IDE — worth verifying `ANTHROPIC_API_KEY` is current); (b) Anthropic API regional latency; (c) something specific to the JPEG content. The point of B1.10 is that the answer becomes immediately visible regardless of which it is.
- **Verification:** `npm run lint` clean. `npm run build` clean (bundle unchanged — server-side only file). 27 log lines confirmed via grep. Anthropic ctor changes confirmed at `api/classify.ts:244-245`. No document content logged anywhere.
- **Sprint SMOKE-001 fully unblocked.** Next first-click upload should either succeed end-to-end (logs show full path) OR fail with clear breadcrumb pointing at the exact stuck step. No more silent hangs in this surface.
- **Commit:** `ab0769c`.

## [Sprint VERCEL-MAX-DURATION — Vercel function timeout]

- **Time:** ~7 min / 15 min
- **Output:**
  - MODIFIED: `api/classify.ts` — added `export const maxDuration = 60` immediately after the existing `export const config = { runtime: 'nodejs' }` (line 36). Comment block explains the 60s ceiling matches the Anthropic SDK's 30s × 1 retry worst case set in B1.10.
- **Why:** Vercel Hobby plan default function timeout is 10 seconds. Anthropic Vision calls for classification typically take 5-15 seconds. Without the explicit export, production deployments would time out before Anthropic responds, causing classification to fail in production even though the code is correct. Free fix on Hobby plan; Hobby max is 60s.
- **Pattern:** This is the "configure for the worst case" side of POL-013 (every external API boundary needs explicit timeout + diagnostic logs). B1.10 set the SDK timeout (30s × 1 retry = 60s); this sprint sets the function timeout (60s). Aligned ceilings — defense in depth.
- **Placement deviation flagged:** prompt suggested placing the export "before imports" but the file's existing convention places `export const config = ...` after imports. ESLint's `import/first` rule would object to non-imports before imports. Placed the new `maxDuration` export immediately after the existing `config` export to keep both Vercel-config exports together. Functionally identical to Vercel — both `config.maxDuration` and standalone `export const maxDuration` are recognized.
- **Verification:** `npm run lint` clean. `npm run build` clean (bundle 525.97 kB unchanged — server-side only). `grep -n maxDuration api/classify.ts` → exactly one occurrence at line 36.
- **Verification needed downstream:** production smoke test from phone (Sprint VERCEL-SMOKE).
- **Commit:** `0bfcba0`.

## [Sprint B1.11 — Node runtime header access fix (ISS-012)]

- **Time:** ~15 min / 30 min
- **Output:**
  - MODIFIED: `api/classify.ts` — added runtime-agnostic `getHeader(request, name)` helper next to `authenticate()`; replaced the failing `request.headers.get('authorization') ?? request.headers.get('Authorization')` (line 399) with a single `getHeader(request, 'authorization')` call (helper normalises to lowercase internally).
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-012 entry (FIXED).
- **ISS-012 → FIXED.** Production `/api/classify` no longer 500s with `TypeError: request.headers.get is not a function`. The Node-runtime header object (plain object, lowercase keys) is now handled correctly; the Edge-runtime `Headers` instance with `.get()` is also still handled — future-proof if anyone flips the runtime config.
- **First production-validated bug of the day.** Local Windows + Norton/schannel SSL revocation issue (POL-014 candidate) had been blocking us from reaching `authenticate()` end-to-end during local dev. Vercel production logs surfaced this immediately on the first successful production hit. Lesson: production logs are sometimes the cheapest diagnostic surface, especially when local tooling is broken.
- **Why this was hidden so long:** ISS-012 has been in `api/classify.ts` since Sprint B2's initial wiring (commit `27b5b1d`). It was masked by the upstream chain of bugs (ISS-005 → ISS-007 → ISS-008 → ISS-009 → ISS-010) plus the local Windows network issue. The TypeScript type for `Request.headers` says `Headers` instance, but Vercel's Node runtime gives a plain object — `tsc` doesn't catch this divergence, only runtime does. Sixth bug today on this surface; second to be a structural API mismatch hidden by upstream blockers.
- **Defense-in-depth:** the new `getHeader` helper handles both runtimes via runtime feature detection (`typeof headers.get === 'function'`). Also defends against Node's occasional `string[]` header values (e.g., `set-cookie`) by taking the first string.
- **Verification:** `npm run lint` clean. `npm run build` clean (bundle 525.97 kB unchanged — server-side only). `grep request.headers. api/classify.ts` returns only a JSDoc historical reference; no runtime call sites. `grep getHeader` returns 2 (definition + single call site).
- **Pattern signal logged in ISS-012:** seven sprints / six issues today on this surface (B1.5 → B1.11). Each unmasked the next bug. Production logs were the diagnostic that finally surfaced ISS-012.
- **Commit:** `8ec72ed`.

## [Sprint VERCEL-MAX-DURATION-FIX — config object merge (ISS-013)]

- **Time:** ~10 min / 15 min (Part A only; Part B runs separately)
- **Output:**
  - MODIFIED: `api/classify.ts` — merged `maxDuration: 60` into the existing `export const config` object. Deleted the standalone `export const maxDuration = 60` line. Single config line at line 40 now: `export const config = { runtime: 'nodejs', maxDuration: 60 }`. Comment block updated to document the ISS-013 root cause (standalone-export silently ignored by non-Next.js Vercel Functions).
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-013 entry (FIXED).
- **ISS-013 → FIXED.** Sprint VERCEL-MAX-DURATION (commit `0bfcba0`) had shipped the standalone-export form on the false assumption it was equivalent to `config.maxDuration`. Production confirmed it wasn't: function ran 300031ms = plan default. Vercel reads only `config.maxDuration` for non-Next.js Functions. Merged form now ensures the 60s ceiling actually takes effect.
- **Why this matters operationally:** every failed production smoke test was costing 5 minutes of wall time instead of the intended 60 seconds. With this fix, the next failure surfaces in 1/5 the time. **5× iteration speed-up** on remaining production bugs.
- **Documentation gap surfaced:** Vercel docs at <https://vercel.com/docs/functions/configuring-functions/duration> show both `export const maxDuration = 60` (Next.js) and `export const config = { maxDuration }` (pure Functions) shapes without prominently flagging that the first only works in Next.js. Worth feedback to Vercel; meanwhile, this codebase's pattern is now explicit. Future polish candidate: a build-time lint asserting `maxDuration` lives in `config` (POL-016?).
- **Pattern signal:** tenth bug today on this surface. Production logs (via Vercel MCP) were again the diagnostic that surfaced the silent-ignore behaviour — local validation can't catch it because local `vercel dev` doesn't enforce maxDuration the same way production does. Reinforces POL-013 ("every external API boundary needs explicit timeout") and adds a sub-lesson: **silent-ignore behaviours by SDKs/runtimes are themselves a defect class worth defending against**.
- **Verification:** `npm run lint` clean. `npm run build` clean (bundle 525.97 kB unchanged — server-side only). `grep -c maxDuration api/classify.ts` → 4 (3 in comments + 1 in config object). `grep maxDuration src/` → 0 (no client imports). `grep 'export const config' api/classify.ts` → exactly 1 occurrence at line 40, including both runtime + maxDuration.
- **Open after this sprint:** the actual hang location for the 08:15:36 production 504 is still unknown. Part B (DIAG-008) below investigates whether B1.10's `[classify]` log lines are visible anywhere in production logs — that's the diagnostic that pinpoints the stuck step. With ISS-013 closed, the next test cycle hits a 60s ceiling, so the answer surfaces in 60s instead of 5 min.
- **Commit:** see git log.

## [Sprint B1.7 — addFiles setState-updater race fix]

- **Time:** ~25 min / 45 min
- **Output:**
  - MODIFIED: `src/features/upload/useUploadBatch.ts` — `addFiles` lifts batch-ID generation and ref assignment OUT of the setState updater per Codex diagnosis
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-007 entry (FIXED)
  - LOGGED: `.claude/STATE-PRJ-improvements.md` — POL-009 (refs-vs-state refactor) + POL-010 (distinguish null reasons), both PLANNED for Phase 1+
- **ISS-007 → FIXED.** First-click upload no longer deterministically fails. `batchIdRef.current` is now assigned synchronously at line 97, before `setState` at line 109; `startUpload` reads the ref on the same event tick and finds it set.
- **B1.5 loud-fail branch preserved unchanged** (lines 125-139) as defense-in-depth for genuine worker-not-resolved cases (RLS denial, network errors). It will no longer fire on first-click; if it does, it's signalling a real auth issue.
- **Codex diagnosis quality:** surgical. Root cause pinpointed to a single line (`useUploadBatch.ts:90`); fix shape clear before any code was written; ~5 lines moved end-to-end. First productive use of the Codex plugin.
- **POL-009 captured:** `useUploadBatch.ts` keeps two parallel sources of truth (`state.workerId`/`workerIdRef` and `state.batchId`/`batchIdRef`) that must be lock-stepped manually. ISS-007 is exactly the failure mode this pattern predicts. Phase 1+ refactor candidates: (a) explicit-parameter passing into `startUpload`/`uploadDocument`, no refs; or (b) `useReducer` over the upload state machine. Either is structurally less buggy.
- **POL-010 captured:** B1.5's null-check branch conflates worker-null vs batch-null into one piece of misleading copy. Post-B1.7, batch-null should be impossible by invariant — POL-010 splits the branch so the impossible case throws loudly (next regression in this shape gets caught fast) and the real auth-failure case keeps the "Account not ready" copy.
- **Process lesson:** B1, B1.5, B1.6 all touched this surface; none browser-smoke-tested the happy path; the bug surfaced only on the first real upload attempt. SMOKE-001 should run BEFORE the next surface of this size lands. Process signal, not just a code bug.
- **SMOKE-001 unblocked:** first-click upload now exercises the full B1+B2 pipeline (storage → classify → routing).
- **Verification:** `npm run lint` clean. `npm run build` clean (1718 modules transformed; bundle 525.80 kB / gzip 149.50 kB — unchanged from B1.6, the fix is pure logic restructuring).
- **Commit:** `558160f`.

## [Sprint B1.8 — useUploadBatch refactor (ISS-008 + POL-009 + POL-010 close simultaneously)]

- **Time:** ~70 min / 120 min (target was 90)
- **Output:**
  - REFACTORED: `src/features/upload/useUploadBatch.ts` — full hook rewritten; `workerIdRef` + `batchIdRef` eliminated; `addFiles` returns `{ entries, batchId }` synchronously; `startUpload` takes explicit `(entries, workerId, batchId)` parameters; B1.5 worker-null loud-fail branch removed; all setState calls now pure functional updaters with no closure side effects.
  - UPDATED: `src/features/upload/UploadZone.tsx` — `onPickFiles` and `onDrop` handlers updated for the new addFiles return shape and explicit startUpload params; drop-zone gating now matches button gating (closes the asymmetry flagged during the ISS-007 review).
  - LOGGED: `.claude/STATE-PRJ-issues.md` — ISS-008 entry (FIXED).
  - UPDATED: `.claude/STATE-PRJ-improvements.md` — POL-009 status PLANNED → IMPLEMENTED; POL-010 status PLANNED → IMPLEMENTED (closed by removal).
  - LOGGED: `.claude/STATE-PRJ-improvements.md` — POL-011 (regression test infra for upload pipeline; Phase 1+).
- **ISS-008 → FIXED.** First-click upload no longer stalls at "Uploading..." indefinitely. Same anti-pattern as ISS-007 (setState-updater side effect read synchronously after the call); refactor removes the entire pattern class instead of patching individual instances.
- **POL-009 → IMPLEMENTED.** Refs-mirroring-state pattern is gone. State is now the sole source of truth. Two refs (`workerIdRef`, `batchIdRef`) eliminated. Future bugs of this shape are not possible because the pattern doesn't exist anymore.
- **POL-010 → IMPLEMENTED (closed by removal).** B1.5's "Account not ready" loud-fail branch was meant to distinguish worker-null vs batch-null. The refactor makes both null states unreachable (TypeScript signature `(entries: FileEntry[], workerId: string, batchId: string)` enforces non-nullable; `UploadZone` handlers gate before calling). Distinguishing is moot when neither is reachable. The defense-in-depth this branch was meant to provide is replaced by structural impossibility.
- **POL-011 captured.** First proposed regression test in PayChecker. Would catch ISS-005, ISS-007, ISS-008 with one test. Deferred to Phase 1+ until test infrastructure (vitest + jsdom + @testing-library/react) is ratified via ADR.
- **Pattern signal logged in ISS-008:** three bugs in `useUploadBatch` in 24 hours, all in the same anti-pattern family. Codex flagged the structural issue during ISS-007 review (POL-009). We deferred. Bill came due in 5 hours. Lesson: when an external review flags a structural pattern, schedule the fix immediately, not "later." POL-009 + POL-010 now landed in the same sprint as ISS-008's closure.
- **Drop-zone gating asymmetry resolved.** Both `onPickFiles` and `onDrop` now triple-gate: `!isResolvingWorker && !workerError && !!workerId` before calling `addFiles`. Symmetric. The pre-B1.8 hole where a drop during the resolve window could create zombie pending rows is closed.
- **Sprint 7 / SMOKE-001 implications:** Sprint 7 onboarding flow UNCHANGED. `api/classify.ts` UNCHANGED. `src/lib/upload.ts` (`uploadDocument`) UNCHANGED. `useClassifyBatch` UNCHANGED. The blast radius of this refactor is exactly two files. SMOKE-001 is now genuinely unblocked: first-click upload should flow through storage POST → `/api/classify` → routing without hitting any null-fail branch.
- **Verification:**
  - `npm run lint` clean
  - `npm run build` clean (1718 modules transformed; bundle 525.97 kB / gzip 149.59 kB; +0.17 kB raw / +0.09 kB gzipped vs B1.7 — slightly more explicit-parameter wiring offset by removed loud-fail branch)
  - `grep -r "workerIdRef" src/` → 0 matches (ref eliminated)
  - `grep -r "batchIdRef" src/` → 0 matches (ref eliminated)
  - `grep -r "Account not ready" src/` → 0 matches (loud-fail branch removed; even the JSDoc historical reference reworded to keep grep clean)
  - `grep -r "@anthropic-ai/sdk" src/` → 0 matches (server/client security boundary holds)
  - `grep -r "@clerk/backend" src/` → 0 matches (server/client security boundary holds)
- **Commit:** see git log.
