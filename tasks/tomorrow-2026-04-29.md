# Tomorrow — 2026-04-29 — Build day starts

Today (2026-04-28) shipped 17 sprints. The architectural pivot is locked end-to-end: ADR-013 ratified, schema applied (Migration 0011), reference docs current, privacy policy drafted, perf cleanup drafted. Tomorrow flips into BUILD mode.

**Read first thing each session:** `CLAUDE.md` → `docs/retros/LATEST.md` → `.claude/STATE-PRJ-issues.md`. Today's daily log in `tasks/today-2026-04-28.md` has the full close-out narrative if you need re-entry context.

---

## First action — Sprint POL-002-APPLY (5 min)

Migration 0012 was drafted at end-of-day yesterday but not applied. Apply it first thing.

**Steps:**
1. Read `supabase/migrations/0012_fk_indexes_and_rls_perf.sql`.
2. Apply via `mcp__claude_ai_Supabase__apply_migration` with the SQL body of Parts 1 + 2 (skip the ROLLBACK comment block; it's file-only). OR run `supabase db push` locally if linked.
3. Run `mcp__claude_ai_Supabase__get_advisors(type='performance')` and verify:
   - `unindexed_foreign_keys` count drops from 9 → 0 on `*_facts` tables
   - `auth_rls_initplan` count drops from 8 → 0
   - `unused_index` count: 13 (the 9 new indexes will appear here at first — they're cold; that's expected at Phase 0)
4. Update `.claude/STATE-PRJ-issues.md`:
   - ISS-002 → FIXED (commit 0012 hash, today's date)
   - ISS-003 → FIXED (same)
5. Update `.claude/STATE-PRJ-improvements.md`:
   - POL-002 → FIXED (commit 0012 hash, today's date)
6. Single commit: `docs(state): close ISS-002/003 + POL-002 post Migration 0012 apply`
7. Push.

**Hard-stop rules for POL-002-APPLY:**
- 10 min total elapsed
- Apply fails on a structural issue (unexpected pre-existing constraint, etc.) → STOP, surface to user, do NOT improvise
- Advisor lints not 0 on the targeted rules → STOP, do not paper over

---

## Sprint B1 — Upload UI + drop zone (90 min BUILD)

After POL-002-APPLY closes, paste this prompt as Sprint B1.

═══════════════════════════════════════════════════════════════════
SPRINT B1 — Upload UI + drop zone (BUILD)
═══════════════════════════════════════════════════════════════════

BUDGET: 90 min HARD CAP. (4 parts.)
GOAL: Build the upload zone UI as the primary entry point per ADR-013. File picker, drop zone, multi-file support, progress feedback. Documents land in storage with canonical filename per `storage-architecture-v01.md`. Classification call deferred to Sprint B2 — UI provides the contract for B2 to consume.

DELIVERABLE:
- `src/lib/upload.ts` updated: `PAYSLIPS_BUCKET = 'documents'` (closes ISS-001)
- Canonical filename generation (`{worker_uuid}/_unclassified/{timestamp}_{disambiguator}.{ext}`)
- Client-side SHA-256 `content_hash` computation (Web Crypto API)
- New route `/upload` with drop zone + file picker
- Multi-file upload with `batch_id` grouping
- Progress feedback per file
- `documents` row inserts with `state='raw'`, `batch_id`, `content_hash`, `worker_facing_name=NULL` (filled by Sprint B2 EXTRACT)
- Hand-off contract for Sprint B2 (a list of `documents.id` values + `batch_id` ready for classification)
- Single commit; deployable to Vercel

CRITICAL RULES — READ FIRST:
- BUILD work, not design — all decisions are locked in `storage-architecture-v01.md` + `extraction-service-v01.md`
- DO NOT modify any architecture v01 doc
- DO NOT modify any migration files
- DO NOT call Anthropic or Voyage APIs (Sprint B2 wires those)
- Apete is the test subject — drop zone copy must be ESL-friendly
- Worker UUID is RLS-required; never expose it in any URL Apete sees

═══════════════════════════════════════════════════════════════════
PART 1 — AUDIT (10 min HARD CAP, 0:00 → 0:10)
═══════════════════════════════════════════════════════════════════

Read in order:
1. `CLAUDE.md`
2. `.claude/INDEX.md`
3. `docs/architecture/decisions.md` (ADR-013)
4. `docs/architecture/document-intelligence-plan-v01.md` (full)
5. `docs/architecture/storage-architecture-v01.md` (filename convention; bucket strategy; PII discipline; idempotency)
6. `docs/architecture/extraction-service-v01.md` (output schemas — what classification will return; cost guardrails: 50 docs/worker/day hard cap, 30/day soft warning)
7. `.claude/ref/REF-DB-schema.md` (current schema post-0011 + 0012)
8. `.claude/ref/REF-API-routes.md` (route stubs section — `/documents/upload` is what we're building)
9. `src/lib/upload.ts` (CURRENT state — first cutover target; line 3: `PAYSLIPS_BUCKET = 'payslips'`)
10. `src/pages/Dashboard.tsx` (existing bucket cards — Sprint D will rewire later; B1 doesn't touch)
11. `src/App.tsx` (routing pattern; protected route example)
12. `src/components/layout/ProtectedRoute.tsx` (Clerk gate)
13. `src/components/ui/Button.tsx` (existing button variants)
14. `src/styles/tokens.css` (52px tap target; AU-friendly tone)
15. `src/types/db.ts` (Migration 0011 types should already be here)
16. `docs/product/personas.md` (Apete primary)

In chat (no file changes yet):

A) WHAT EXISTS (don't rebuild)
- `src/lib/upload.ts` already has `uploadPayslip()`, `ensureWorker()`, `signPayslipUrl()`. The MIME validation + 10MB cap + worker_id-as-first-segment pattern are all reusable.
- Existing path pattern: `{worker_id}/{uuid}-{safeFilename}` — needs to become `{worker_id}/_unclassified/{YYYY-MM-DDTHHMMSS}_{disambiguator}.{ext}` per storage-architecture-v01.md.

B) DECISIONS LOCKED (no design work)
- Bucket: `documents` (Migration 0011); `payslips` retained as alias.
- Filename: `{worker_uuid}/_unclassified/{ISO-timestamp}_{4-hex}.{ext}` — this is pre-CLASSIFY; Sprint B2 will move to `{type}/` after classification.
- Hash: SHA-256 hex via Web Crypto API (`crypto.subtle.digest('SHA-256', ...)`) on the file's ArrayBuffer.
- batch_id: one fresh UUID per upload session (multi-file picker tap = 1 batch).
- documents.state: `'raw'` on insert.

C) DECISIONS THIS SPRINT MUST MAKE (small UX calls)
- Drop zone vs file picker — both? (recommend: drop zone with "or tap to choose files" tertiary; mobile-first = file picker is the dominant path)
- Worker-visible progress shape (per-file bar? batch-level "X of N done"?)
- After upload finishes: navigate where? (recommend: stay on /upload; show "uploaded N files; we're reading them" placeholder; Sprint B2 wires the classification view)
- Camera capture (mobile <input capture="environment">)? Yes — Apete is mobile-first.
- HEIC support (iOS) — already in Migration 0011 bucket allowed_mime_types. Pre-flight client-side check.

D) PRESSURE-TEST CANDIDATES
- Apete on regional Wi-Fi uploading a 9MB photo — progress + resumability
- Apete uploads the same payslip twice — content_hash UNIQUE catches it; UX surfaces "you already uploaded this"
- Apete picks a HEIC photo on iOS — accepted by bucket; client-side dedup hash works
- Apete cancels mid-upload — partial documents row? (decision: only INSERT documents row AFTER storage upload succeeds; cancel = nothing in DB)
- Apete uploads wrong file (e.g., a screenshot of TikTok) — rejected by classifier, not B1's job

═══════════════════════════════════════════════════════════════════
PART 2 — BUILD (60 min HARD CAP, 0:10 → 1:10)
═══════════════════════════════════════════════════════════════════

Files to create / modify:

**Modify `src/lib/upload.ts`:**
- Rename `PAYSLIPS_BUCKET` → `DOCUMENTS_BUCKET = 'documents'` (closes ISS-001)
- Keep a `PAYSLIPS_BUCKET` alias `= 'documents'` for the 1-release deprecation window OR add a TODO comment for clean removal once verified no other call sites exist (verify before removing)
- Add `computeContentHash(file: File): Promise<string>` using Web Crypto API
- Add `buildCanonicalPath(workerId: string, file: File): string` returning `{workerId}/_unclassified/{ISO-ts}_{4-hex}.{ext}`
- Add `uploadDocument(file, supabase, workerId, batchId?): Promise<{documentId, storagePath, contentHash}>` — generalizes `uploadPayslip` for any doc_type; passes `doc_type='other'` (CLASSIFY will set the real type later); inserts `documents` row with `state='raw'`, `batch_id`, `content_hash`
- Keep `uploadPayslip` as a thin wrapper for any legacy call sites (deprecation path)

**Create `src/features/upload/UploadScreen.tsx`:**
- Drop zone using HTML5 drag-and-drop (`onDragOver`, `onDrop`)
- File picker via hidden `<input type="file" multiple accept="image/*,application/pdf,image/heic" capture="environment">` (capture only on mobile; desktop ignores)
- Tertiary "or tap to choose files" link triggers picker
- On file(s) selected/dropped:
  - Generate `batch_id = crypto.randomUUID()` once for the batch
  - For each file: compute content_hash, call uploadDocument, track progress in local state
  - Show per-file row: filename + status (`uploading` / `uploaded` / `dedupe` / `error`)
- Footer: "Uploaded {N} files in this batch. Reading them now…" once batch complete
- Tertiary navigation back to /dashboard

**Create `src/pages/Upload.tsx`:**
- Thin page wrapper that mounts `UploadScreen`
- Uses `ProtectedRoute` pattern from existing routes

**Modify `src/App.tsx`:**
- Add route: `/upload` → `<ProtectedRoute><Upload /></ProtectedRoute>`
- (Dashboard re-routing is Sprint D; B1 just adds the route)

**Copy guidelines (Apete-grounded):**
- Headline: "Add documents" (not "Upload" — too generic)
- Body: "Take a photo or pick files from your phone. We'll read them and you check what we found."
- Empty state: "Drop files here, or tap to choose"
- Per-file states: `Uploading…` / `Uploaded` / `Already uploaded` / `Couldn't upload — try again`
- Done state (sage tint per ADR-012 AFTERMATH): "Got {N} files. We're reading them — check back in a minute."

**Dedup handling (per `storage-architecture-v01.md`):**
- On UNIQUE constraint violation `(worker_id, content_hash)`: catch the error, mark the file row as `Already uploaded`, surface a tertiary "Open the existing one" link (TODO until B2 has the open-document view).
- Don't fail the whole batch if one file dedupes; continue with the rest.

═══════════════════════════════════════════════════════════════════
PART 3 — VERIFY (15 min HARD CAP, 1:10 → 1:25)
═══════════════════════════════════════════════════════════════════

1. `npm run build` — pass with zero errors
2. `npm run lint` — pass
3. Vite dev server boots clean
4. Manual smoke test (10 scenarios):
   a. Navigate to `/upload` — screen renders
   b. Drag a single PDF onto drop zone → status `Uploading…` → `Uploaded`
   c. Verify via Supabase MCP: documents row exists with `state='raw'`, `batch_id` set, `content_hash` populated, `storage_path` matches `{worker_uuid}/_unclassified/...`
   d. Verify storage bucket: object present at the canonical path
   e. Drop the SAME file again → status `Already uploaded` (UNIQUE INDEX caught it)
   f. Drop 3 different files at once → all get the same `batch_id`; all complete
   g. Tap "or tap to choose files" → file picker opens
   h. On mobile (test via Vercel preview): camera capture works (`capture="environment"`)
   i. Reload page mid-batch (close + reopen) → completed uploads persist; in-flight ones are gone (acceptable per the "cancel = no DB row" rule)
   j. `git status -s` — only `src/`, `tasks/`, possibly types/db.ts touched

5. ISS-001 closes when this sprint commits.

═══════════════════════════════════════════════════════════════════
PART 4 — COMMIT (5 min HARD CAP, 1:25 → 1:30)
═══════════════════════════════════════════════════════════════════

Single commit:
```
feat(upload): upload zone + drop zone + dedup (Sprint B1)
```

Body should cover:
- Closes ISS-001 (`PAYSLIPS_BUCKET` → `DOCUMENTS_BUCKET`)
- Implements canonical filename per `storage-architecture-v01.md`
- SHA-256 content_hash for client-side dedup (Web Crypto API)
- Multi-file batch upload with `batch_id` grouping
- documents rows inserted with `state='raw'`; Sprint B2 picks up CLASSIFY
- Browser smoke test 10/10 scenarios passed (or honestly flagged otherwise)
- Co-Authored-By: Claude Opus 4.7

DO NOT push until user reviews + approves.

═══════════════════════════════════════════════════════════════════
HARD-STOP RULES
═══════════════════════════════════════════════════════════════════

- 90 min total elapsed
- Build / lint failing
- Schema change needed (out of scope — flag and stop)
- Anthropic / Voyage call attempted (B2's job)
- Dashboard re-routing attempted (Sprint D's job)
- Migration files modified

═══════════════════════════════════════════════════════════════════
FINAL REPORT (Sprint B1)
═══════════════════════════════════════════════════════════════════

When done:
- Time per part + total
- Files created / modified
- ISS-001 closed: yes/no
- Build / lint / dev boot: pass / fail
- Browser smoke 10 scenarios: pass count + honest report
- Commit hash
- Sprint B2 readiness: hand-off contract clear (B2 reads `documents WHERE state='raw'`)
- Approval requested before push

---

## Sprint B2/B3/D/E — out of tomorrow's first session

After B1 ships clean, the next sprint queue (each gets its own day-start prompt block when ready):

**Sprint B2 — Classification service wiring (90 min):** Anthropic Haiku 4.5 call per `extraction-service-v01.md`; reads `documents.embedding IS NULL AND state='raw'`; writes `document_classifications` row; updates `documents.state` based on confidence-bucketed routing.

**Sprint B3 — Routing review UI (90 min):** worker-correction surface for the 0.50–0.85 confidence band; updates `document_classifications.routing_status='worker_corrected'`; triggers Layer 2 + Layer 3 memory writes.

**Sprint D — Dashboard retrofit (60 min):** rewire bucket cards to `/upload`; closes the form-vs-upload-first inconsistency; removes `payslips` bucket alias once verified safe (closes POL-003).

**Sprint E — Comparison engine v1 (90 min):** consume confirmed `*_facts` rows + run the calc against `award_rates` + `award_allowances`; produce immutable `comparisons` snapshot. This is the pay-day sprint — first time Apete sees a real number.

Don't paste these prompts ahead of time; each sprint's brief should reflect the post-B1 state of the codebase and any surprises.
