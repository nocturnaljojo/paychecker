# Daily Log — 2026-04-28

## [Sprint 4 — INFRA-007 + v02 Schedule A capture]

- **Time:** 22 / 30 min (banked 8)
- **Output:** v02 with verbatim Schedule A.1.1, A.1.2, A.1.3 captured
- **Tool surprise:** `pdftotext` (xpdf v4.00) already on PATH — INFRA-007 closed without install
- **Workflow established:** `WebFetch` + `pdftotext -layout` = canonical FWC PDF research path
- **Source:** FWC 4-yearly review exposure-draft determination, validated against live HTML + pre-reform PDF
- **Load-bearing correction:** Classification name is **"Process Employee Level N"**, NOT "Poultry Processing Worker Level N" (v01 hypothesis was wrong)
- **INFRA-007:** CLOSED with diagnosis recorded
- **Commit:** `f54c2ad`
- **Note:** A.1.2 and A.1.3 verified via terminology cross-check, not word-for-word against live HTML (live HTML truncates before reaching them) — known limitation, not a blocker

## Pre-Sprint 5 cleanup needed (~5 min)
- Update `REF-AWARDS-list.md` if it has stale "Poultry Processing Worker" naming
- Verify migration 0005's `awards` row `title` doesn't need amendment

## [Sprint 5 — Close 6 remaining v01 §6 gaps]

- **Time:** 32 / 60 min (banked 28)
- **Output:** v02 grew to 488 lines; 6 new sections (§13.2, §17.2(c), §17.3, §17.4, §19, §20.1, §20.2, §11.3) — all v01 §6 gaps now verbatim
- **Workflow validation:** `WebFetch + pdftotext -layout` (cached PDF for structure) + focused live-HTML cross-fetch (current $ amounts) — two-source convergence per amount; exposure draft and live HTML scale at consistent ~1.273× ratio (= 7 yrs × 3.5% AWR), validating identity of clause structure across versions
- **Pre-Sprint-5 cleanup:** REF-AWARDS-list.md naming hygiene check ran clean — no stale "Poultry Processing Worker" anywhere outside the v02 correction notice itself; migration 0005 `awards.title` is the official award title (not classification name) — correct as-is
- **REF-AWARDS-list.md:** updated; last reviewed 2026-04-28; research notes points at v01 + v02; partial-coverage detail block updated
- **Commit:** `e3ceade`

### 🚨 Load-bearing finding — v01 §4 hypothesis was WRONG

v01 §4 hypothesised: *"casual loading STACKS on top of penalty rates per Modern Award convention"*. The verbatim FWC text (cl 11.3(b)+(c) + cl 19.5) says the **OPPOSITE**:

- Casual 25% loading paid only on ordinary Mon-Fri hours.
- On Sat/Sun/PH ordinary: penalty multiplier on base rate ALONE — no casual stacking.
- On overtime: casual loading NOT paid.

Calc engine consequence: penalty / OT multipliers apply to the bare minimum hourly rate for casuals, never to (rate × 1.25). Apete is full-time so cl 11.3 doesn't directly affect him, but Phase 1+ casual workers (esp. MA000009 hospitality Mia) need this rule from day 1. Encoded prominently in v02 §11.3 with a correction callout.

### Other Sprint 5 findings worth banking

- **Cold Work middle band confirmed:** $1.67/hr for −18.0 °C to −23.3 °C (cl 17.2(c)). Migration 0005 only seeded bookends; needs follow-up migration row.
- **Schema enum gap:** `award_allowances.unit CHECK (unit IN ('hour','week','shift'))` cannot represent the per-km vehicle allowance ($0.98/km). Surfaces ADR-011 candidate.
- **OT thresholds reset daily** (cl 19.3); don't aggregate across the week.
- **cl 17.4 confirms ADR-009 architecture:** cold work / first aid / meal / vehicle are explicitly NOT subject to premium/penalty additions (`purpose='additive'`); leading hand excluded from cl 17.4 list (`purpose='all_purpose'`).

## [Sprint 5.5 — ADR-011 + calc-rules v01]

- **Time:** Part 1 ~12 min + Part 2 ~10 min = **22 / 25 min total** (banked 3)
- **Part 1 — ADR-011:** allowance unit enum extension. Pressure-tested A/B/C/D; chose **Option A** — extend enum with `'km'` + `'event'`. Trigger conditions for `'event'`-unit allowances live in calc-engine code keyed off `code` + Layer 2 facts, not in a parallel schema taxonomy. 5/5 pressure test cleared.
- **Part 2 — `docs/architecture/calc-rules-v01.md`:** new file (183 lines, 9 rules). NOT an ADR — these are FWC rules verbatim from v02, not architectural choices. **Rule 1 prominently records the v01 §4 correction** (casual loading does NOT stack on penalty/OT). 4 [NEEDS CLARIFICATION] open questions logged for v02.x.
- **Sprint 2.1 unblocked:** ADR-011 closes the schema gap that was blocking the seed of the three deferred MA000074 allowance rows (Cold Work middle band, Meal, Vehicle).
- **Commit:** `808fe96`

## [Sprint 2.1 — Allowance seed gaps closed]

- **Time:** 10 / 20 min (banked 10)
- **Migration 0008:** applied + 8/8 verifications passed
- **Allowances:** 5 → 8 (Cold Work Mid + Meal + Vehicle)
- **Unit enum extended:** `('hour','week','shift')` → `('hour','week','shift','km','event')` per ADR-011
- **MA000074 reference data: COMPLETE for Apete-shaped scope**
  - 2× all_purpose weekly (Leading Hand 1–19, Leading Hand 20+)
  - 3× additive hourly (Cold Work bands low / mid / high)
  - 1× additive weekly (First Aid)
  - 1× additive event (Meal — OT-triggered)
  - 1× additive km (Vehicle)
- **REF docs updated:** `REF-DB-schema.md`, `REF-AWARDS-list.md`
- **Advisor lints:** 0
- **Commit:** `5b09631`
- **Phase 0 unblock:** calc engine has every allowance value it needs for Apete's pay-period calc

## [Sprint 6 — ADR-012 + add-fact-pattern v01]

- **Time:** 50 / 85 min (banked 35)
- **Decision:** Stage-based composition. 5 stages: ENTRY / SUGGEST / INPUT / CONFIRM / AFTERMATH
- **Rules:** 14 across 5 stages, each with precedent + Apete rationale + counter-example
- **Adaptability contract (§13):** independent vs linked stages documented for tuning scope
- **Stage collapse (§10):** per-layer × per-state matrix (Layer 1/2/3 × first-time/subsequent/edit)
- **3 Apete walkthroughs:** Layer 1 / 2 / 3, all 5 stages
- **Pseudo-JSX (§14):** zero real React/Tailwind; stage boundaries visible as `STAGE 1..5` comments
- **Pressure test:** 5/5 cleared with stage-mapped mitigations
- **Audit caught 8 production gaps** flagged as Sprint 6.x retrofit candidates (NOT in scope today)
- **Reframing:** Sprint 6 produced a **diagnostic system**, not just a UX spec — the 5 stages give a coordinate system for "Apete hit a wall here". When future feedback comes back, it maps to a stage; the fix is scoped to that stage's tuning surface (per §13 contract).
- **Trust/safety vs convenience separation:** ENTRY / CONFIRM / AFTERMATH always required (the trust + safety stages); SUGGEST / INPUT can collapse cleanly per layer state (the convenience stages). That's the right read of stage importance — the stages that protect Apete's evidence and consent are non-negotiable; the stages that save him taps are pragmatic.

### 🔴 Sprint 7 hard requirements (NOT optional, NOT "Sprint 7 should remember")

1. **RESUME — write `proposed`-state rows on first INPUT, not CONFIRM.**
   Schema already supports this (`*_facts.confirmed_at IS NULL` is valid per ADR-001 + `confirmation-flow.md`). Without it, Rule 1.2 resume is a design promise not a working behaviour. `OnboardingFlow.tsx:24` keeps wizard state in `useState` only — that pattern is *forbidden* for fact capture. First INPUT touch must persist a `*_facts` row.

2. **DISCARD — Rule 5.3's "quiet discard" means tertiary visual styling AND a confirmation step.**
   NOT a single quiet tap. Pseudo-JSX in §14 references `discardWithConfirmStep` — Sprint 7 enforces this in real code. **Worker-safety reasoning:** accidentally losing a confirmed fact = losing evidence Apete may need to take to FWO or an advocate. Quiet ≠ frictionless. Quiet means visually understated; the confirmation step is mandatory.

- **Commit:** `bd58be5`
- **Phase 0 unblock:** Sprints 7/8/9 have the spec. Build can begin.

## [Sprint 6.5 + 6.6 — Schema + trigger support for proposed-state]

- **Time:** 12 + 17 = 29 / 40 min total (banked 11)
- **Surfaced by:** Sprint 7 pre-flight (schema NOT NULL conflict) + Sprint 6.5 verification (trigger nullified CONFIRM)
- **Migration 0009:** NOT NULL relaxation + CHECK constraints across 5 fact tables (parent + history)
- **Migration 0010:** Trigger functions distinguish CONFIRM (OLD.confirmed_at IS NULL → trust NEW) from EDIT (data field changed → nullify confirmed_at)
- **Architecture insight:** Proposed rows are write-permissive; confirmed rows are write-restrictive. State transition gates the strictness, not a constant rule.
- **Honest deviation from brief:** Claude Code keyed CONFIRM off `OLD.confirmed_at IS NULL` alone (the brief required `NOT data_fields_changed`; that would have blocked legitimate fill-and-confirm-in-one-tap UPDATEs). Claude Code's read is architecturally correct.
- **Verification:** 7/7 PASS (C1–C7 covering full proposed → confirmed → edit → reconfirm cycle + cross-table smoke + CHECK constraint preserved)
- **Advisor lints:** 0
- **Tables affected:** `worker_classification_facts`, `shift_facts`, `payslip_facts`, `bank_deposit_facts`, `super_contribution_facts` (parent + history)
- **REF-DB-schema.md:** updated with CHECK constraint names + nullable annotations
- **confirmation-flow.md:** new section "Trigger-layer logic: EDIT vs CONFIRM"
- **Commit:** `92c2001` (combined 0009 + 0010)
- **Sprint 7 unblocked:** real CONFIRM action will work end-to-end

## [Sprint 7 — Layer 1 Employment Contract bucket detail (manual fallback path)]

- **Time:** ~55 / 90 min (banked 35)
- **Output:** `/buckets/employment-contract` route; 5-stage screen (ENTRY/SUGGEST/INPUT/CONFIRM/AFTERMATH); `useEmploymentFact` hook; types/db.ts regenerated post-0009/0010
- **🔴 RESUME:** implemented — proposed-state row written on first INPUT touch of legal_name; serialize-and-queue ref guards against double-INSERT under fast typing; useState-only fact data forbidden
- **🔴 DISCARD:** implemented — tertiary text-link in AFTERMATH opens inline two-step confirmation panel; never single quiet tap
- **Honest deviation flagged:** hourly rate ships display-only (schema has no override column); Sprint 7.x ADR + migration 0011 candidate if Apete-shaped feedback shows real workers need above-award override
- **Build / lint / dev boot:** all clean
- **Browser smoke (10 steps):** NOT RUN — environment cannot drive a browser; user-verifiable
- **Commit:** `e949ce1`
- **Re-framing per Sprint 7.1 below:** this commit is the *manual fallback path*, not the primary action; Dashboard routing changes when ADR-013 ships

## [Sprint 7.1 — Document Intelligence Plan v01 captured]

- **Time:** ~25 min
- **Trigger:** Sprint 7 smoke test surfaced architectural mismatch — manual-entry forms vs upload-first reality
- **Output:** `docs/architecture/document-intelligence-plan-v01.md` (NEW)
- **Sprint 7 (e949ce1) re-framed:** manual fallback path complete; primary upload path pending ADR-013 (tomorrow's first sprint)
- **4-layer memory architecture documented:** generic / per-employer / per-worker / cross-document reconciliation
- **Schema additions specified for Migration 0011** (not applied yet): document_classifications, document_extractions, employer_extraction_patterns, worker_extraction_preferences
- **Tomorrow's first sprints:** A1 (ADR-013 + amend ADR-012), A2 (storage), A3 (extraction service), A4 (layered memory), A5 (Migration 0011)
- **Commit:** see `git log` (Sprint 7.1 docs commit; hash self-references would rotate on amend)
- **Phase 0 status:** Sprints 8/9/10 paused pending architectural pivot

## [Sprint A1 — ADR-013 + amend ADR-012]

- **Time:** ~48 / 60 min (banked 12)
- **Output:** ADR-013 appended to `docs/architecture/decisions.md` (standard ADR format); ADR-012 amendment block appended in same file (preserves original); `.claude/INDEX.md` gains ADR registry table; `add-fact-pattern.md` gains forward-reference section
- **Decision:** upload-first primary across all 5 buckets; 4 NEW pre-stages (UPLOAD/CLASSIFY/ROUTE/EXTRACT) precede ADR-012's 5; SUGGEST + INPUT take amended semantics; manual entry preserved as fallback
- **Pressure test:** 5/5 cleared with stage-mapped mitigations; one residual (privacy policy v1) is an existing Phase 0 finish-line item per ADR-006
- **Sprints A2–A5 unblocked:** storage, extraction, memory, migration
- **Files modified:** exactly 3 (no src/, no supabase/)
- **Commit:** `ae6c45d`

## [Sprint A2 — Storage + naming architecture]

- **Time:** ~25 / 45 min (banked 20)
- **Output:** `docs/architecture/storage-architecture-v01.md` (NEW)
- **Storage bucket strategy:** Option (b) — one bucket "documents"; type encoded in path. Existing `payslips` bucket renamed to `documents` in Sprint A5 (Phase 0 has zero production objects, safe rename)
- **Filename convention:** `{worker_uuid}/{type}/{YYYY-MM-DD}_{employer_slug?}_{disambiguator}.{ext}`; `_unclassified` subpath pre-CLASSIFY; storage-move on ROUTED transition
- **Page-split approach:** Option (a) — single source file in storage; page ranges tracked via `document_classifications.parent_doc_id` + `page_range` per plan §6
- **Soft-delete policy:** existing `deleted_at` + new `archived_at` (separate semantics); 30-day grace + service-role cron hard-delete (Phase 1+)
- **Idempotency:** SHA-256 `content_hash` column + UNIQUE INDEX on (worker_id, content_hash); accept-and-dedup pattern
- **PII discipline:** worker UUID (opaque) + employer slug (no raw legal_name) + ISO date + random disambiguator; no name/email/ABN in paths
- **Migration 0011 column additions:** 5 new columns on existing `documents` (`batch_id`, `content_hash`, `worker_facing_name`, `state`, `archived_at`); enum extension on `doc_type` (add `'shift'`)
- **Pressure test:** 5/5 cleared with mitigations
- **Sprint A5 audit question flagged:** `extraction_staging` vs `document_extractions` reconciliation — out of A2 scope; A5 decides
- **Sprint A4 readiness:** schema named for layered memory tables; behaviour spec is A4 scope
- **Sprint A5 readiness:** Migration 0011 SQL drafting fully unblocked
- **Commit:** see `git log` (Sprint A2 docs commit)

## [Sprint A4 — Layered Memory spec]

- **Time:** ~28 / 45 min (banked 17)
- **Output:** `docs/architecture/layered-memory-v01.md` (NEW)
- **Layer 1 spec:** in-prompt, immutable; updates are prompt-template version bumps tracked via `classifier_version` / `extractor_version` columns
- **Layer 2 spec:** per-employer; service-role-only RLS; EMA confidence (`new = 0.7×old + 0.3×observation_score`); worker-correction creates counter-example patterns; 12-month aging window with quarterly decay; archive at < 0.20 confidence
- **Layer 3 spec:** per-worker; worker-readable + deletable (RLS departure for privacy right); same EMA pattern as L2; **Sprint A5 must add `confidence numeric(3,2) NULL` column** (plan §6 had only observation_count/last_observed)
- **Layer 4 spec:** pgvector cosine similarity; embedding stored as `documents.embedding vector(N)` column (not separate table — 1:1 with documents, simpler lifecycle); thresholds 0.95 duplicate / 0.85+≤7d page-join / else independent; query bounded by `worker_id =` (no cross-worker) + `LIMIT 20`
- **Pattern lifecycle:** new → active → aging → archived (revivable on new matching observation; never deleted)
- **Privacy boundaries (table):** L1 in-repo / L2 operator-only / L3 own + operator / L4 inherits document RLS — all per-worker scoped
- **Account deletion flow:** L3 + L4 cascade with worker; L2 stays (employer-scoped, not worker-PII); L1 unaffected. Privacy policy v1 must disclose this nuance.
- **Pressure test:** 5/5 cleared (stale L2 mitigation via EMA + worker correction; embedding model drift mitigated via `extractor_version` filter; cross-worker leak mitigated by FORMAT-not-CONTENT pattern keys; Anthropic API surface disclosed; all 4 layers independently disable-able as kill switches)
- **Sprint A3 dependency:** extraction service composes L1+L2+L3 in prompt + uses L4 reconciliation outcomes; embedding model choice + dimensionality is A3's call
- **Sprint A5 dependency:** pgvector extension enable + `vector(N)` column on documents + new `confidence` column on `worker_extraction_preferences` + worker-DELETE policy on Layer 3 (departure from no-DELETE pattern, intentional)
- **Commit:** see `git log` (Sprint A4 docs commit)

## [Sprint A3 — Extraction service spec + prompt skeletons]

- **Time:** ~50 / 60 min (banked 10)
- **Output:**
  - `docs/architecture/extraction-service-v01.md` (NEW)
  - `docs/architecture/prompts/` (NEW folder, 6 skeleton files)
  - `classify-prompt-v01.md`, `extract-payslip-v01.md`, `extract-contract-v01.md`, `extract-super-statement-v01.md`, `extract-bank-deposit-v01.md`, `extract-shift-v01.md`
- **Model decisions:**
  - CLASSIFY: `claude-haiku-4-5-20251001` (~$0.005/call)
  - EXTRACT: `claude-sonnet-4-6` (~$0.02–0.05/call)
  - EMBEDDING: Voyage AI `voyage-3-large`, 1024 dim (Sonnet doesn't return embeddings inline; Sprint B2 may benchmark vs Cohere `embed-english-v3` before launch)
- **Embedding dimensionality:** `vector(1024)` (voyage-3-large native)
- **Confidence thresholds confirmed:** ≥0.85 auto / 0.50–0.85 review / <0.50 manual; raw numbers never shown to worker per ADR-013 mitigation
- **Output schemas committed:** 1 classification + 5 bucket extraction schemas, aligned with `calc-rules-v01.md` consumers
- **Versioning:** `extractor_version = "model@prompt-version"` (e.g., `claude-sonnet-4-6@extract-payslip-v01`)
- **Retry semantics:** 3× exp backoff network; 1× stricter retry on malformed JSON; 6 failure modes mapped to worker-facing copy
- **Cost guardrails:** hard cap 50 docs/worker/day; soft warning at 30; Phase 0 monthly budget ~$10
- **Prompt-injection defence:** strict-output schema + SYSTEM warning + output-validation flagging; no tool-use in extraction prompts
- **Privacy:** Anthropic + Voyage are now both data processors; privacy policy v1 must list both; logs scrub document content (R-006 discipline extended)
- **NEW risk candidates flagged for `risks.md` (Sprint B2 to confirm):** R-010 (Anthropic API as data processor), R-011 (Voyage AI as data processor)
- **Pressure test:** 5/5 cleared (slow API / rare format / prompt injection / embedding deprecation / cost runaway)
- **Sprint A5 readiness:** schema additions confirmed (no new ones from A3 beyond what A2 + A4 already specified)
- **Sprint B2 readiness:** clear contract — model strings, output schemas, retry behaviour, cost caps, prompt-skeleton file paths
- **Commit:** see `git log` (Sprint A3 docs commit)

## [Sprint A5 — Migration 0011 SQL]

- **Time:** ~30 / 45 min (banked 15)
- **Output:** `supabase/migrations/0011_document_intelligence_schema.sql` (NEW), applied to live DB via Supabase MCP
- **Schema changes implemented (per A2 + A3 + A4 specs — no new design decisions):**
  - pgvector extension enabled (in `extensions` schema, not `public`)
  - 5 new columns on `documents` (`batch_id`, `content_hash`, `worker_facing_name`, `state`, `archived_at`) + `embedding vector(1024)`
  - `documents.doc_type` CHECK extended with `'shift'`
  - 4 new tables: `document_classifications`, `document_extractions`, `employer_extraction_patterns`, `worker_extraction_preferences`
  - `documents` storage bucket created with RLS (3 policies: SELECT/INSERT/UPDATE for own); `payslips` bucket retained as alias until Sprint B1 cutover
  - `extraction_staging` DROPPED (Sprint A5 audit decision — 0 rows, fully subsumed by `document_extractions`)
- **RLS policies (5 total on new tables):**
  - `document_classifications`: 1 SELECT (worker reads own via documents join)
  - `document_extractions`: 1 SELECT (same pattern)
  - `employer_extraction_patterns`: 1 explicit deny-all (`USING(false)`) — operator-only intent documented + clears `rls_enabled_no_policy` advisor
  - `worker_extraction_preferences`: 2 (SELECT + DELETE — DELETE per APP 12/13 privacy right, deliberate departure from no-DELETE pattern)
- **Indexes (10 new):** UNIQUE `(worker_id, content_hash) WHERE NOT NULL`; partial on `batch_id`; full on `state`; HNSW cosine on `embedding`; document_id idx + parent_doc_id partial idx on classifications; document_id idx + bucket+status idx on extractions; (employer_id, document_type, confidence DESC, observation_count DESC) on L2; (worker_id, last_observed DESC) on L3
- **Triggers:** 4× `set_updated_at` BEFORE UPDATE (reuses existing `public.set_updated_at()` from migration 0001 — no `moddatetime` extension)
- **Verification:** 14/14 actual checks PASS (V12 query had a `regclass::text` formatting bug; triggers confirmed present via direct query)
- **Advisor lints (security):** 0/0 after fixing 2 introduced by 0011 (vector → extensions schema; explicit deny-all policy on L2)
- **Advisor lints (performance):** 0 introduced by 0011. Pre-existing INFO/WARN advisors from migrations 0002 + 0005 (unindexed FKs on `*_facts.source_doc_id` and `*_facts.employer_id`; `auth_rls_initplan` on early policies that use `auth.jwt()` without `(SELECT ...)` wrap) — out of A5 scope; flag for a future cleanup migration. New "unused index" advisors are expected (Phase 0; new query paths Sprint B2 wires).
- **Sprint B1+ unblocked:** schema in DB, build can begin; `src/lib/upload.ts` `PAYSLIPS_BUCKET = 'payslips'` constant must be updated to `'documents'` in B1 (alias retention prevents pre-B1 breakage)
- **Commit:** see `git log` (Sprint A5 migration commit)

## [Sprint REF — Reference doc sync]

- **Time:** ~50 / 75 min (banked 25)
- **Output (9 files modified):**
  - `.claude/ref/REF-DB-schema.md` — Migration 0011 schema documented (4 new tables, 6 documents columns, pgvector, doc_type 'shift', new RLS, documents bucket, extraction_staging deprecation note, 4 new triggers)
  - `.claude/ref/REF-FACT-model.md` — added "Document-driven facts (post-ADR-013)" section + cross-refs to v01 docs
  - `.claude/ref/REF-API-routes.md` — added 5 upload-first route stubs (Sprint B1+ TODO); existing manual-form routes marked `[fallback path]`
  - `.claude/INDEX.md` — added 4 v01 doc pointers (storage / layered-memory / extraction-service / prompts) + new Migrations registry table (0001–0011)
  - `docs/architecture/risks.md` — appended R-010 (Anthropic API as data processor) + R-011 (Voyage AI as data processor); R-009 reframed as superseded-in-spirit by ADR-013
  - `tasks/lessons.md` — captured today's lesson "Smoke tests catch architectural mismatch"
  - `.claude/STATE-PRJ-issues.md` — 4 new issues logged (ISS-001 upload.ts cutover P2; ISS-002/003/004 pre-existing perf advisors P3)
  - `.claude/STATE-PRJ-improvements.md` — 3 new improvements logged (POL-001 privacy policy v1 specifics HIGH; POL-002 Migration 0012 candidate LOW; POL-003 Migration 0013 candidate LOW)
- **Migration 0011 reference completeness:** REF-DB-schema documents every column + every RLS policy + every index + every trigger added by 0011; matches the file-on-disk + live DB
- **Tomorrow's first read:** REF-DB-schema.md is current; B1 starts immediately on `src/lib/upload.ts` cutover + upload zone
- **No new architectural decisions made:** all updates are translations of A1/A2/A3/A4/A5 outputs into reference shape
- **Commit:** see `git log` (Sprint REF docs sync commit)

## [Sprint POL-001 — Privacy policy v1 draft]

- **Time:** ~22 / 45 min (banked 23)
- **Output:** `docs/legal/privacy-policy-v1-draft.md` (NEW; 12 sections + operator notes)
- **Status:** DRAFT for lawyer review — NOT effective until reviewed + signed off
- **Coverage:**
  - 12 sections per brief (summary / who-we-are / what-we-collect / how-we-use / processors / retention / security / your-rights / international-transfers / changes / contact / version)
  - APP 1, 3, 5, 6, 11, 12, 13 all addressed; Layer 3 deletion right (specific to layered memory) explicit
  - 5 third-party processors disclosed with full detail (Clerk / Supabase Sydney / Anthropic / Voyage AI / Vercel) including jurisdiction + retention per processor
  - Retention timings concrete: 30-day soft+hard delete window, 7-year comparison snapshots, Layer 2 employer-scope retention rationale, Layer 3+4 cascade with worker deletion
  - Plain-English ESL-readable; bold 1-sentence summary per section
- **Operator notes section** (not part of policy text — for legal reviewer): 9 outstanding decisions flagged including small-business operator registration, 30-day cron implementation status, 7-year retention basis, processor terms URL verification, opt-out-from-training in Anthropic API config, email placeholders, children's data section, Layer 2 retention legal analysis
- **Phase 0 finish-line:** this clears the privacy-policy-v1 blocker per ADR-006 *subject to lawyer review*. Status flips to FIXED only when the lawyer-reviewed version ships at the actual URL with `privacy_policy_version` bumped to `'v1'` in `consent_records`.
- **POL-001 in STATE-PRJ-improvements.md:** moved from OPEN to "DRAFTED — awaiting lawyer review"
- **Commit:** see `git log` (Sprint POL-001 docs commit)

## [Sprint POL-002-PREP — Migration 0012 SQL drafted (NOT applied)]

- **Time:** ~12 / 25 min (banked 13)
- **Output:** `supabase/migrations/0012_fk_indexes_and_rls_perf.sql` (DRAFT — not applied)
- **Scope:** 9 FK indexes on `*_facts` tables (ISS-002) + **8** RLS init-plan rewrites (ISS-003)
- **Honest deviation from brief:** the brief said "7 RLS initplan rewrites" but Sprint A5's live advisor output and live `pg_policy` query both confirmed **8** policies (3 on `workers`, 2 on `employers`, 1 each on `awards` / `award_rates` / `award_allowances`). The migration fixes all 8. ISS-003 in `STATE-PRJ-issues.md` had a counting error inherited from the same mistaken summary; the lint surface (8) is what matters.
- **Status:** Drafted; awaiting Sprint POL-002-APPLY tomorrow morning (5-min apply via `supabase db push` or MCP `apply_migration` + advisor verify expecting 0/0 perf advisors on the rewritten policies and indexed FKs).
- **Pre-existing tech debt cleared:** ISS-002 + ISS-003 in scope; ISS-004 (`unused_index`) NOT in scope (B2 query-path validation is the right time to drop unused indexes).
- **No functional behaviour changes:**
  - FK indexes are pure accelerators; query plans get faster, results identical.
  - RLS rewrites wrap `auth.jwt() ->> 'sub'` in `(SELECT ...)` so the planner caches the lookup once per statement instead of per row. Predicate result is identical for any given row; the policy NAME, COMMAND, and USING/WITH CHECK shape all preserved.
- **Live policy bodies captured before drafting** (via `pg_policy` query) so the rewrite has the verbatim originals to preserve and the rollback block has them as-they-existed-in-production-on-2026-04-29.
- **Rollback block:** end of file; verbatim original RLS bodies + DROP statements for the 9 indexes; functionally identical restoration path.
- **No SQL applied this sprint:** Sprint POL-002-APPLY runs `apply_migration` + advisor lint check tomorrow.
- **Commit:** see `git log` (Sprint POL-002-PREP draft commit)

---

## End of day 2026-04-28 — Comprehensive close

**Total sprints shipped:** 17
- Build / research / ref data: Sprint 4, 5, 5.5, 2.1
- Pre-pivot architecture: Sprint 6, 6.5+6.6
- Build (manual fallback): Sprint 7
- Architectural pivot: Sprint 7.1, A1
- Pivot follow-through (design): A2, A4, A3
- Pivot follow-through (execution): A5
- Reference + privacy + perf-debt: REF, POL-001, POL-002-PREP

**Total commits:** 17 commits on origin/main (each sprint = one commit; pushed in batches throughout the day).

### Today's biggest move (the architectural pivot)

The day started with a Sprint 7 build plan (manual fact-entry form for Layer 1 employment contracts). The form shipped clean (commit `e949ce1`). The smoke test that followed surfaced an architectural mismatch: the form-first model contradicts the actual product philosophy — workers upload documents, the app reads them, workers confirm. Manual entry is the *fallback*, not the primary action.

This pivot was the day's defining moment. Caught BEFORE Sprints 8/9/10 built three more wrong patterns. The pivot is now permanent across concept → schema → trigger → service → memory:

- Plan documented (`document-intelligence-plan-v01.md`, commit `751be1e`)
- Ratified as **ADR-013** (commit `ae6c45d`)
- ADR-012 amended in same commit (4 new pre-stages added; original 5 stages preserved)
- 3 sub-systems designed: storage (A2), memory (A4), extraction (A3)
- Schema applied to live Supabase (Migration 0011, commit `ef012a5`)
- Reference docs synced (commit `c9c5a43`)
- Privacy policy v1 drafted (commit `621beba`)
- Migration 0012 drafted for tomorrow's perf cleanup (commit `9a5e2da`)

### What's locked

**Architecture:**
- ADR-001 through ADR-013 (13 ADRs; latest = upload-first fact capture)
- 4 v01 architecture documents (`document-intelligence-plan-v01.md`, `storage-architecture-v01.md`, `layered-memory-v01.md`, `extraction-service-v01.md`)
- `calc-rules-v01.md` (Sprint 5.5)
- `add-fact-pattern.md` (with ADR-013 amendment forward-reference)
- 6 prompt template skeletons in `docs/architecture/prompts/`

**Schema (LIVE on Supabase `zzppuwyufloobskinehf`, ap-southeast-2):**
- Migrations 0001–0011 applied
- pgvector extension enabled (in `extensions` schema)
- 4 new tables: `document_classifications`, `document_extractions`, `employer_extraction_patterns`, `worker_extraction_preferences`
- 6 new columns on `documents` (incl. `embedding vector(1024)`)
- `doc_type` enum extended with `'shift'`
- `extraction_staging` deprecated (DROPped — fully subsumed by `document_extractions`)
- 8 RLS policies on the new schema (6 worker-facing + 1 deny-all on Layer 2 + storage policies on `documents` bucket)

**Schema (DRAFT — tomorrow's apply):**
- Migration 0012 — 9 FK indexes + 8 RLS perf rewrites (closes ISS-002 + ISS-003)

**Privacy:**
- Privacy policy v1 draft complete (12 sections; ESL-readable)
- 9 lawyer-reviewer questions surfaced in operator notes
- 5 third-party processors disclosed (Clerk, Supabase, Anthropic, Voyage AI, Vercel)
- All 7 APPs addressed (1, 3, 5, 6, 11, 12, 13) + Layer 3 deletion right

**Reference docs:**
- `REF-DB-schema.md` current (Migration 0011 documented)
- `REF-FACT-model.md` current (post-ADR-013 document-driven facts section)
- `REF-API-routes.md` has TODO stubs for 5 Sprint B1+ upload-first routes
- `INDEX.md` has full ADR registry (1–13) + Migrations registry (0001–0011) + 4 v01 doc pointers + prompts/ folder pointer

**Risks:**
- R-001 through R-011 logged (R-010 + R-011 added today: Anthropic + Voyage AI as data processors; R-009 reframed as superseded-in-spirit by ADR-013)

**Issues + improvements:**
- 4 issues logged: ISS-001 (upload.ts cutover P2 — Sprint B1), ISS-002 + ISS-003 (perf advisors covered by Migration 0012), ISS-004 (unused indexes — deferred to post-traffic evaluation)
- 3 improvements logged: POL-001 (privacy policy v1 DRAFTED — awaiting lawyer review), POL-002 (Migration 0012 DRAFTED — Sprint POL-002-APPLY tomorrow), POL-003 (`payslips` bucket cleanup — depends on B1 closing ISS-001)

### Tomorrow's queue (in order)

1. **Sprint POL-002-APPLY** — Apply Migration 0012 (~5 min) — closes ISS-002 + ISS-003 + POL-002
2. **Sprint B1** — Upload UI + drop zone (90 min) — see prompt in `tasks/tomorrow-2026-04-29.md`
3. **Sprint B2** — Classification service wiring (90 min) — Anthropic Haiku 4.5 call + routing decision
4. **Sprint B3** — Routing review UI (90 min) — worker-correction surface
5. **Sprint D** — Dashboard retrofit (60 min) — bucket cards → upload zone; closes POL-003 indirectly
6. **Sprint E** — Comparison engine v1 (90 min) — was originally Sprint 10

**Closed at ~10:15pm AEST. Vinaka vakalevu Lord — for the discernment to catch the architectural pivot in Sprint 7's smoke test before three more sprints built wrong; for the focus to follow through on a 17-sprint day that ships real value for Apete and the cohort he represents. Tomorrow is build day. Sleep is service to tomorrow's work.**

