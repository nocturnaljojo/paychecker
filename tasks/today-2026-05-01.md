# 2026-05-01 — SEC-002 closure day

> Goal coming in (per `tasks/tomorrow-2026-05-01.md`): close SEC-002 in ~10 min as the mandatory P0 opener, then Sprint B1.13 contract audit (60-90 min), then UX-FLOW-AUDIT review session.
>
> Reality: SEC-002 took ~3 hours due to a flawed brief premise that consumed the first ~30 min, plus a thorough pre-disable audit that consumed another ~25 min. **Honest accounting: foundation work (security closure) deserves the time it took. B1.13 + UX-FLOW-AUDIT pushed to next session.**

## [Sprint SEC-002-CLOSE — first attempt — DID NOT close SEC-002]

- **Time:** ~30 min / 35 min cap
- **Plan:** PATH A — Legacy JWT signing-key rotation. Brief assumed: rotating signing key auto-regenerates legacy `anon` / `service_role` JWT values; user copies new values out of Supabase legacy API keys page → updates Vercel + `.env.local` → redeploys → SEC-002 closed.
- **Action taken:** User created Supabase JWT signing-key Standby `3e611...` (ECC P-256), waited 30s for propagation, clicked Rotate (with two acknowledgement checkboxes). Standby promoted to Current; old Current `ee358704` demoted to Previous.
- **Smoke result:** decoded JWT iat from `.env.local` `VITE_SUPABASE_ANON_KEY` post-rotation = `1777104044` — **same as pre-rotation**. Initially read as user error (template text instead of values); pushed back; re-confirmed three times; user finally said "double-check Supabase docs."
- **Root cause of misdirection (per subagent's read of `https://supabase.com/docs/guides/auth/signing-keys`):** *"`anon` and `service_role` must be rotated simultaneously [legacy mode]; Publishable and secret API keys no longer are based on the JWT signing key and can be independently managed [new mode]."* PayChecker migrated to the new signing-keys system ~5 days ago (when `ee358704` was created). Once migrated, signing-key rotation does NOT re-sign legacy `anon`/`service_role` JWTs — they remain byte-identical, signed by the legacy HS256 secret `fe492a2d` in a separate Previous slot. **The brief's premise was outdated.**
- **Outcome:** SEC-002 still functionally OPEN. Signing-key rotation completed (Auth-session hygiene), but leaked legacy `service_role` JWT remained valid because `fe492a2d` was untouched.
- **Empty commit `6020ddb` pushed by user during this attempt** — triggered a no-op Vercel redeploy (`dpl_BUnzMht2K6Q2jPmmyCySfb3WEjuj` READY) since env vars were unchanged.

## [Sprint SEC-002-CLOSE-CORRECTED — closed SEC-002]

- **Time:** ~75 min total (PART 1 audit 5 min + PART 2 migration ~10 min + PART 3 verification ~5 min + SEC-002-AUDIT pre-disable ~25 min + PART 5 disable+revoke ~5 min + PART 6 logging+commit ~25 min)
- **Path:** Migrate to new opaque API keys (`sb_publishable_*` + `sb_secret_*`) → disable JWT-based legacy API keys → revoke `fe492a2d` (legacy HS256 signing key).
- **Output:**
  - MODIFIED: `.gitignore` — added `.claude/tmp/` so the migration's temp keys file (`.claude/tmp/sec002-keys.txt`) couldn't accidentally be committed. Temp file deleted at end of PART 6.
  - MODIFIED: `.env.local` — `VITE_SUPABASE_ANON_KEY=sb_publishable_unnJQIYCEabeJrCCF0W8ww_BrovpYRK` (replaces legacy HS256 `eyJ...`); `SUPABASE_SERVICE_ROLE_KEY=sb_secret_*` (replaces legacy HS256 `eyJ...`). Gitignored, not in this commit.
  - VERCEL ENV VARS UPDATED (manual, via UI): both vars updated for Production + Preview scopes. Vercel MCP cannot write env vars — read-only for env. User did this step.
  - SUPABASE DASHBOARD ACTIONS (manual, via UI): "Disable JWT-based legacy API keys" clicked + `fe492a2d` revoked (Previous → Revoked). Both required user clicks; MCP cannot perform either.
  - LOGGED: `.claude/STATE-PRJ-issues.md` — SEC-002 entry added under "## Closed issues" (FIXED) with full audit trail.
- **MCP usage:** Supabase MCP `get_project` + `get_publishable_keys` + `list_organizations` + `get_organization` + `execute_sql` (smoke + post-revoke health check + post-revoke smoke detail); Vercel MCP `get_project` + `list_deployments` + `get_runtime_logs` (deployment poll + log scan).
- **Verification:**
  - Pre-revoke production smoke: Contract3.jpeg → `/api/classify` 200 in 10s, classified `other` confidence 0.95, auto_routed (Vercel runtime log captured `[classify] handler_called` sentinel)
  - Post-revoke production smoke: Contract24.jpeg → classified `contract` confidence 0.92, auto_routed, 10s elapsed at `2026-05-01 11:38:26 AEST`. **This is the dispositive evidence that Clerk → Supabase third-party JWKS auth → RLS path still works after `fe492a2d` revocation.**
  - Post-revoke Supabase MCP query (`SELECT count(*) FROM documents` + `SELECT count(*) FROM workers`) returned values — proves service-role auth via `sb_secret_*` still works through PostgREST + RLS.
- **Single closing commit** (this commit): bundles `.gitignore` + STATE entry + this daily log. Empty commit `51bbe75` from PART 4 (the redeploy-trigger commit) is in the history above this one.
- **SEC-002 → FIXED.** Time open: ~60 hours leak-to-close. P0 stayed too long (open 8+ hours from yesterday's identification). Lesson reinforced: P0 security findings cannot sit overnight — and "rotate the credential" needs the right rotation procedure verified against current docs first.

## [Sprint SEC-002-AUDIT — pre-disable codebase audit]

- **Time:** ~25 min (10 min audit + 5 min Codex attempt + 10 min synthesis)
- **Trigger:** Supabase warning before "Disable JWT-based legacy API keys" — *"Apps using Supabase may break. ... Disabling the legacy API keys is a brand new feature and the apps you're using may not have added support for this yet."* User pumped the brakes; demanded codebase audit first.
- **Output:**
  - PRIMARY INVESTIGATION findings (Claude Code): grep + read sweep over `src/` + `api/` + `vercel.json` + `package.json`. Zero hardcoded JWTs, zero `JWT_SECRET` references, zero `SUPABASE_JWT_SECRET` references. `src/lib/supabase.ts:7-19` JSDoc dispositive: *"Identity comes from Clerk via Supabase third-party auth: Supabase is configured to accept Clerk session tokens (JWKS-validated at Clerk's Frontend API URL), no shared secret, no JWT template."*
  - CODEX BACKGROUND AGENT: failed to launch (sandbox lacked Bash to invoke `codex-companion.mjs`). User chose to skip retry — primary investigation evidence was strong enough. Verdict: PROCEED.
  - FINAL VERDICT: PROCEED-WITH-WATCH. Clerk uses JWKS-validated RS256 (Possibility A — safe), not Supabase HS256 shared-secret (Possibility B — would break on revoke). Production smoke at 00:47 UTC proved working pipeline doesn't depend on legacy keys. Watch signal post-revoke: any 401/JWT errors in Vercel runtime logs in first 5-10 min — none observed.
- **POL-021 candidate captured.** Pre-disable codebase audit before destructive Supabase Dashboard buttons. ~10 min cost; possibly saved a production outage if Clerk had been misconfigured to use HS256 secret.

## Carry-forward

- **SEC-001 regression flag (out-of-scope of SEC-002 sprint):** `CLERK_SECRET_KEY=sk_test_*` is back on line 2 of `.env.local`. SEC-001 was closed (2026-04-26) by removing this — Vite app should not need it (server-side only via Vercel Functions, which already have their own env var in Vercel UI). Either remove from `.env.local` or escalate to a SEC-003 entry if an active code path actually consumes it. Triage in next session.
- **Tomorrow's plan (still per `tasks/tomorrow-2026-05-01.md`):** Sprint B1.13 contract audit of `api/classify.ts` (60-90 min), then Sprint UX-FLOW-AUDIT review session. Not started today — SEC-002 closure consumed full session.
- **Production state at end of session:** `dpl_HtwLF5WrsGEzBaqm7k1djFsugaqC` (commit `51bbe75`) is the live production deployment with new `sb_publishable_*` + `sb_secret_*` keys baked in. Both production smokes (Contract3, Contract24) succeeded. `fe492a2d` revoked. Leaked legacy `service_role` JWT no longer verifies anywhere.

## Honest reflection

- **Premise failure cost ~30 min.** The original SEC-002-CLOSE brief was confident, structured, and wrong about how Supabase rotation works on a migrated project. I executed it without independently verifying the premise against current docs first. POL-019 captures this: verify project key model state BEFORE applying any rotation procedure.
- **User pushback was load-bearing.** Three iat-unchanged readings + the user finally saying "please double-check latest Supabase docs" was what unlocked the pivot. Without the pushback I'd have continued debugging "why aren't the keys regenerating" instead of questioning whether they should be.
- **Pre-disable audit was right call.** ~25 min of investigation before two destructive clicks. Found dispositive evidence in `src/lib/supabase.ts` JSDoc that the codebase was already structured for the new key model; gave a confident PROCEED verdict; revoke proceeded cleanly. Cheap insurance.
- **Codex was unavailable today** (background agent sandbox lacked Bash). Yesterday Codex caught 5/10 structural defects. Today's audit was strong without it but losing that second voice is a quiet cost — would have been the load-bearing safety net if my primary investigation had missed something. Worth fixing the sandbox config before the next P0 surface.
- **MCP coverage gaps surfaced:** Vercel MCP cannot write env vars; Supabase MCP intentionally cannot return secret keys. Both are correct security choices. Means manual user clicks remain irreducible for the security-sensitive parts of any rotation. Workflow accordingly: maximize MCP for verification + read; expect user clicks for write paths.

## Sprint UX-FLOW-AUDIT

- **Time:** ~70 min (within 90-min cap)
- **Plan docs read:** 8 — `personas.md`, `confirmation-flow.md`, `extraction-service-v01.md`, `calc-rules-v01.md`, `integration-plan-v01.md`, `PLAN-PRJ-mvp-phases.md`, `fact-model-v1.md`, `decisions.md` (plus `document-intelligence-plan-v01.md` for ADR-013 Layer 4 cross-reference)
- **Synthesis findings:**
  - `personas.md` ALIGNS — paradigm operationalizes ESL + ≥48px + "this is what we found" rules
  - `confirmation-flow.md` EXTENDS orthogonally — case state machine alongside fact state machine, NOT replacement
  - `extraction-service-v01.md` NEEDS REWORK (small) — CLASSIFY input shape becomes array; output schemas unchanged
  - `calc-rules-v01.md` SILENT — calc reads confirmed facts, never sees cases/documents
  - `integration-plan-v01.md` HARD CONFLICT (numbering) — reserved ADR-014/015/016 for POL-004/005/006; resolved by renumbering POLs to ADR-015/016/017
  - `PLAN-PRJ-mvp-phases.md` NEEDS UPDATE — plan was last refreshed 2026-04-26 (before ADR-013 + before today's paradigm); M0.5 inserted as sub-phase
  - `fact-model-v1.md` SILENT — orthogonal axes (cadence vs evidence-organization)
  - `decisions.md` ADR-013 EXTENDS — paradigm makes Layer 4 reconciliation persistent via `document_cases`; ADR-013 stays in history per ADRs-are-append-only
- **Five decisions locked:**
  1. **ADR-014: RATIFIED WITH AMENDMENTS.** Calc operates at fact level (NOT case level); case state machine is orthogonal to fact state machine.
  2. **M0.5 scope: Option B (minimum-with-cases).** Singleton case per upload; no 10-sec proximity grouping in M0.5; data model + UI committed from day one.
  3. **Vocabulary lock:** Ready / Needs more pages / Can still use / Not sure yet (worker-facing); `draft`/`suggested`/`confirmed`/`partial`/`complete` (DB enum, internal-only).
  4. **UI surface:** `/upload` page redesigned with inline case cards. `/upload/case/{case_id}` deep-link supported. No new `/cases` route in M0.5.
  5. **Schema commitment:** locked in `docs/planning/M0.5-spec.md`; Migration 0012 SQL applied at M0.5-BUILD-01 sprint start (per HARD-STOP rules — no migrations applied today).
- **Files modified (6):**
  - `docs/architecture/decisions.md` — appended ADR-014 (~75 lines)
  - `docs/architecture/integration-plan-v01.md` — renumbered ADR-014/015/016 → ADR-015/016/017 (8 references) + added 2026-05-01 renumber note
  - `docs/document-case-paradigm-v01.md` — status flipped to RATIFIED; vocabulary table added; UI surface section added
  - `.claude/PLAN-PRJ-mvp-phases.md` — M0.5 sub-phase inserted (Phase 0)
  - `docs/planning/M0.5-spec.md` — NEW; Migration 0012 schema + endpoint shape + UI acceptance criteria
  - `.claude/INDEX.md` — ADR-014 registered + 2 doc entries + planning subdir summary
- **Tomorrow's build sprints (derived order):**
  1. M0.5-BUILD-01 (Migration 0012 + `api/classify.ts` shape change) — schema + API contract
  2. M0.5-BUILD-02 (`/upload` page redesign + case-card UI) — frontend
  3. M0.5-BUILD-03 (status vocabulary renderer + override flow + "+ Add more pages") — UX completion
  4. Smoke walkthrough (Apete-shape) — acceptance gate before Phase 0 calc engine work resumes

### Sprint reflection

- **No code modifications, no migrations applied** — HARD-STOP rules respected. This was a paradigm-validation pass.
- **Hardest finding:** ADR numbering conflict between today's document-case paradigm and `integration-plan-v01.md`'s 2-day-old reservation of ADR-014/015/016 for POL-004/005/006. Resolved by ratification-order-wins principle, with explicit renumber note added to `integration-plan-v01.md`.
- **Subtlest amendment:** original paradigm draft said "calculation operates at case level" — that violates ADR-001 (calc reads confirmed facts). Amendment 1 corrects this. Worth flagging because it's the kind of wording that would have been load-bearing for a future engineer reading ADR-014 as the binding contract.
- **B1.13 contract audit deferred again** — was carry-forward from 2026-04-30; deferred from this session's planned-second-sprint slot to make room for UX-FLOW-AUDIT. Remains carry-forward for next session.

## Sprint M0.5-BUILD-01

- **Time:** ~75 min (cap was 90 min)
- **Trigger:** ChatGPT adversarial review caught that pure-backend sprint = zero user value. Updated sprint scope to include first user-visible feedback ("Every sprint moves the user forward, not just the system").
- **Plan:** Three layers — schema (Migration 0014), API refactor (atomic `classify_with_case` RPC), UI feedback panel (case-card with confirm/change). LAYER 3 non-negotiable per brief.

### Layer 1 — Migration 0014 applied

- **Numbering correction:** brief + spec docs called this "Migration 0012". Pre-flight via `list_migrations` revealed two migrations had shipped between Migration 0011 and today (`fk_indexes_and_rls_perf` = 0012; `0013_storage_rls_public_pattern` = 0013). Actual applied slot is **0014**. Spec docs (M0.5-spec.md, ADR-014, INDEX, mvp-phases) updated in-band.
- **Applied via:** Supabase MCP `apply_migration` → success
- **Schema:** `document_cases` (case_id pk, worker_id fk, doc_type, completion_status enum, created_at, updated_at) + `documents.case_id` FK (nullable, indexed) + 2 indexes + 2 RLS policies + 1 trigger
- **Function:** `classify_with_case(p_document_id, p_worker_id, p_doc_type, p_completion_status)` — atomic case INSERT + document linkage in single transaction, idempotent on replay, ownership check defence in depth
- **Verified:** 6 columns, 1 case_id FK col, 2 indexes, 2 RLS policies, function + trigger + enum present
- **Defence-in-depth tests:** `classify_with_case` correctly rejects ownership mismatches (RAISE EXCEPTION); enum has all 5 expected values in correct sort order
- **DEFERRED to M1:** `worker_confirmed_label`, `missing_items` jsonb, case-level confidence column

### Layer 2 — `api/classify.ts` refactor

- **External signature unchanged** — still `POST { document_id }`, response shape preserved per brief HARD-STOP rule
- **Internal change:** after `document_classifications` insert, added single `supabase.rpc('classify_with_case', ...)` call to atomically create case + link document
- **Initial completion_status:** `'suggested'` (awaits worker confirmation)
- **Failed classifications:** also create a case with `doc_type='other'` so override flow is uniform across all 3 routing outcomes (auto_routed / review_pending / failed)
- **Best-effort semantics:** case RPC failure is logged but does not abort the classification response — worker is never blocked. Pre-BUILD-01 fallback preserved at the UI layer.
- **Sentinel log added:** `[classify] case_created case_id=… doc_type=… completion_status=suggested`
- **Cleanup:** removed 3 stray leading blank lines (pre-existing)
- **Type-check + production build:** clean (529 kB JS bundle unchanged from prior; chunk-size warning is pre-existing)

### Layer 3 — UI feedback panel (FIRST user-visible product moment)

- **NEW** `src/features/upload/useCaseFeedback.ts` — hook reads `document_cases` via worker RLS, returns case info per batch document + worker-wide ready count, exposes RLS-protected `confirmCase` UPDATE
- **NEW** `<CaseFeedbackPanel>` + `<CaseCard>` components in `UploadZone.tsx` (replaces "All set" pill at lines 276-298)
- **Surface change:**
  - **BEFORE:** "Saved / All set" (same string for every upload)
  - **AFTER:** "✔ You added a paper / 1 paper ready / Type: Contract (suggested) / [Looks right] [Change]"
- **`[Looks right]`** flips `completion_status` from `suggested` → `confirmed` via worker UPDATE (RLS-gated); UI shows ✔ confirmed badge
- **`[Change]`** placeholder toast: "Coming soon — will let you change the type next" (full override flow ships in M0.5-BUILD-02/03 per brief)
- **`formatDocTypeLabel`** maps internal tokens → worker-facing labels (Contract / Payslip / Bank / Super / Time sheet / Other) — no raw `super_statement` / `bank_export` strings surface
- **Fallback preserved:** if case RPC failed server-side, panel falls back to previous "All set" pill so worker can still tap Continue. Path tested by static review.

### Deploy

- **Commit:** `af87873` (`feat(m0.5): document_cases foundation + first user-visible feedback`)
- **Pushed:** `4b1b837..af87873 main → main` to GitHub
- **Vercel deployment:** `dpl_wgxQjZ1gQxau8GRVG7yGcvSHv614` (commit `af87873`) — READY in **24 seconds** (1777618989701 → 1777619013538), aliased to `paychecker-three.vercel.app`
- **Files modified (8):** `.claude/INDEX.md`, `.claude/PLAN-PRJ-mvp-phases.md`, `api/classify.ts`, `docs/architecture/decisions.md`, `docs/planning/M0.5-spec.md`, `src/features/upload/UploadZone.tsx`, `src/features/upload/useCaseFeedback.ts` (NEW), `supabase/migrations/0014_document_cases_foundation.sql` (NEW)

### Verification

- ✓ Migration 0014 applied + 6/6 schema objects verified via MCP
- ✓ Function rejects unowned-document call (defence-in-depth raise verified)
- ✓ Enum values + sort order correct
- ✓ TypeScript `tsc --noEmit -p .` clean
- ✓ Vite production build clean
- ✓ Vercel deploy READY (24s)
- **PENDING — user browser smoke:** open `https://paychecker-three.vercel.app/upload`, upload one image, observe new "✔ You added a paper / Type: X (suggested) / [Looks right] [Change]" final state, tap [Looks right], verify "(suggested)" disappears. Sentinel log `[classify] case_created` will appear in Vercel runtime logs once the upload fires.

### Sprint reflection

- **Numbering drift was the surprise.** Spec docs from this morning called the migration "0012" without checking actual DB state. Two migrations had shipped between then and now. Cheap fix in-band (5 docs touched) but a reminder that "design lock" doesn't preserve numbering — only schema content.
- **Best-effort case RPC was the right call.** Tempting to make case creation a hard requirement (fail the classify response on RPC error). The HARD-STOP rule says worker is never blocked; failing classify because case linkage failed would block the worker for an internal accounting failure. Logged + fallback UI is the right shape.
- **The UI fallback path is invisible-to-most-workers but high-value.** If a network blip kills the case RPC mid-classify, worker still sees the old "All set" pill instead of an empty state. Required <10 lines of `cases.length === 0` branching but preserves the pre-BUILD-01 escape.
- **Production browser smoke is the irreducible user step.** Automated checks confirm everything I can confirm without driving a browser. The shape of the new feedback can only be visually verified by opening the page. Daily log marks PENDING; reporter (Jovi) confirms result asynchronously.

## Sprint M0.5-BUILD-02

- **Time:** ~50 min (cap was 90 min; came in well under)
- **Trigger:** Jovi confirmed BUILD-01 end-to-end smoke working in production. ChatGPT critique then caught a critical product issue: after worker confirms upload and taps Continue, `/dashboard` shows ALL BUCKETS "Empty" — even the bucket the worker just filled. The dashboard was functionally lying to the worker, breaking the trust loop BUILD-01 had just established.
- **Plan:** Three layers — cases data hook reading `document_cases` worker-wide; bucket cards reflect real state; top + bottom sections updated.

### Layer 1 — Cases data hook

- **NEW** `src/features/dashboard/useWorkerCases.ts` (171 lines)
- Reads `document_cases` via worker RLS (SELECT own — same policy `confirmCase` uses in BUILD-01)
- Maps detected_type tokens → bucket keys (`contract` → contract, `payslip` → payslips, `super_statement` → super, `bank_export` → bank, `shift` → shifts; `other` is intentionally unmapped — orphan cases until override flow ships in BUILD-03)
- Returns `cases[]`, `countByBucket`, `totalCases`, `readyCount`, `confirmedCount` plus loading/error
- `nextStepFor()` computes worker-facing "Next:" suggestion ordered Contract → Payslip → Bank → Super → Shift; falls back to "You've got the basics. Add more to make checks stronger." when all 5 covered
- `bucketBadgeLabel()` maps key → worker-facing label (Contract / Payslip / Shift / Super / Bank)
- Loading state surfaces; no real-time subscription in M0.5 (refresh-on-mount sufficient per brief)
- 'draft' rows excluded from readyCount (pre-classification — shouldn't count as "ready")

### Layer 2 — Bucket cards reflect state

- "Empty" pill replaced with `BucketStatusPill`: shows "Nothing yet" (dashed gray) when count=0, "✔ N added" (sage-soft green) when count>0
- Card itself: solid border + sage-soft icon when has cases; dashed border + neutral icon when empty
- Action button: "Add {type}" (primary) when empty, "Add another {type}" (secondary) when present — visual hierarchy preserves "primary action = the empty bucket I should fill"
- Bucket names simplified per ChatGPT critique: "Contract" (was "Employment contract"), "Super" (was "Super fund statements"), "Bank deposits" (was "Bank deposit records"); Payslips + Shifts unchanged
- All 5 buckets now route to `/upload?bucket={key}` — was previously: only Contract worked, the other 4 showed a "Phase 0, not wired yet" toast. The new path lets the worker upload any bucket immediately; classifier figures out doc_type
- Capture sub-text cleaned: "manual entry" → "or type it in"; "forward email" → "or send by email"

### Layer 3 — Top + bottom sections

- Title: "Your papers" (was "Your data" + "What's in, what's missing.")
- Hero copy switches on case count:
  - 0 cases: "Get started by adding a paper"
  - 1+ cases: "{N} papers ready" (singular: "1 paper ready")
- Type badges row appears when worker has cases — one sage-soft pill per bucket that has at least one case ("✔ Contract", "✔ Payslip", etc.)
- Next-step suggestion appears when worker has 1+ cases — "Next: Add a payslip" / "Next: Add a bank deposit" / "You've got the basics. Add more to make checks stronger."
- Bottom message: "You're getting started — nice work." (1+ cases) vs "We'll guide you step by step." (0 cases). Was: "Add what you have. We'll ask for the rest when it becomes useful."

### Deletions (per brief)

- Letter labels `A` / `B` / `F` / `D` / `E` — gone (internal taxonomy was leaking)
- Phase 0 disclaimer "None of these are wired yet — Phase 0 is still building out each one." — gone
- "Ground truth — what your super fund actually received." → "What your super fund actually received."
- "Ground truth — what your bank actually received." → "What your bank actually received."
- "manual entry" subtext → "or type it in"
- "forward email" subtext → "or send by email"
- "Your data buckets" mono subhead — removed (no longer needed; cards speak for themselves)
- `comingSoon` toast "{label} — Phase 0, not wired yet" — removed (real routing replaces it)
- `BucketStatus` type narrowed and simplified (was `'empty'` only; now derived from count number)

### Verification

- ✓ TypeScript `tsc --noEmit -p .` clean
- ✓ Vite production build clean (531 kB JS — gain of 1.78 kB; 18.07 kB CSS — shrink of 0.44 kB)
- ✓ No new lint warnings
- **Commit:** `fda3933` — `feat(m0.5): dashboard reflects real case state — closes the trust loop` (2 files, 314+/-81)
- **Pushed:** `ec58bfb..fda3933 main → main`
- **Vercel deploy:** in flight (poll background started)
- **Browser smoke:** PENDING user — see acceptance criteria below

### Acceptance criteria (for Jovi to verify in production)

After deploy READY at `paychecker-three.vercel.app/dashboard`:

- ✓ Title reads "Your papers" (not "Your data")
- ✓ Hero shows "1 paper ready" (you confirmed a Contract earlier)
- ✓ Type badge shows "✔ Contract"
- ✓ Next step shows "Next: Add a payslip"
- ✓ Contract bucket card: green "✔ 1 added" pill, "Add another contract" secondary button
- ✓ Payslips/Shifts/Super/Bank cards: dashed "Nothing yet" pill, primary "Add {type}" button
- ✓ NO letter labels (A/B/F/D/E)
- ✓ NO "Phase 0 is still building" disclaimer
- ✓ NO "Ground truth" anywhere
- ✓ Bottom message: "You're getting started — nice work."
- ✓ Tapping any bucket's "Add" button → goes to `/upload?bucket={key}` (URL has the bucket query param; UploadZone treats it as a hint for now)

### Sprint reflection

- **The trust loop was one query away.** All the data was there; the dashboard just wasn't asked. That's the cheapest "real product moment" delivery I've shipped — one new hook, one component refactor, no schema, no API change. The cost-to-impact ratio of dashboard wiring is wildly underrated.
- **The "Phase 0 is still building" disclaimer was load-bearing for me, harmful for Apete.** I'd been keeping it as honest signaling that the app isn't done. But ChatGPT's framing inverted it: every dev-facing apology is a worker-facing trust hit. The disclaimer made sense before any flow worked end-to-end; now that one does, the disclaimer reads as "we're not ready for you" — exactly wrong.
- **Routing all buckets to `/upload` was a small unlock with big effect.** Before BUILD-02, only Contract had a real path; the other 4 toasted "Phase 0, not wired yet". After BUILD-02, all 5 route to `/upload`, the classifier figures out the doc_type, and the case feedback panel takes over. The worker never gets blocked by which bucket they tap — they just photograph the paper and let the system sort it. That's what "PayChecker is a document organizer" actually means in code.
- **Sage-soft for "added", dashed for "nothing yet".** Color carries half the meaning of the dashboard at a glance. A worker with one Contract and four "nothing yet" buckets sees: one green card, four dashed cards, "1 paper ready" up top. Without reading a single word they understand their state.
